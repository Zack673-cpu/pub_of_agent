# 学习方案（主线①：全栈 + 智能体）

> 本文件管"学什么、什么顺序、怎么算完成"；怎么学见 `.qoder/rules/coding-guidelines.md`（教练守则）。

---

## 0. 总目标

**独立产出完整项目**——功能完整、结构规范、可部署、可持续迭代。

核心标准：**每一行代码（无论谁写的）都能说出为什么存在。**
手写是刻意练习手段，不是终点；练成之后你负责架构决策和验收，AI 可以帮你堆量。

毕业考试（三问全过才毕业）：
1. 有一个拿得出手的完整项目（可部署、有真实功能、有文档）。
2. 任意模块随机抽问"这段为什么这么写"，答得上来。
3. 能独立排查生产级 Bug（AI 只当橡皮鸭，不给答案）。训练机制＝每周固定 1~2 次 bug 排查训练（时段见 §2 周末加餐，规则见 bug 排查师守则）；硬前置＝台账第 19 项第一批「最小证据链」必须先落地。

---

## 1. 整体路线

```
上半场（已完成）：热身 + 压舱石三件套——手写 RAG → 裸 API → 丑前端 → 删掉默写重写
下半场（进行中）：完整项目（教材智能体）逐模块推进 → 扩展补齐 + 部署 + 文档 + 复盘
```

### 阶段零~四（已完成）

看课 + 知识闸门 → 30 行手写 RAG → FastAPI 裸接口 → 极简前端 → 删掉重写默写版。关键取舍：先 RAG 后 Agent、全栈从后端开始、禁区（厚框架/多智能体/向量库调优）阶段一~四不碰。

### 阶段五：选定项目 + 架构图（已完成）

- 完整项目＝教材智能体（出版社智能体）。
- 语言＝Python。
- 项目方案 `pub_plan/教材智能体方案设计.md` 仅设计参考、不是交付规格。
- 交付规格＝`pub_plan/产品功能设计定稿.md`：前端与后端照它实现、照它逐条验收。与蓝图分工——蓝图讲"是什么、为什么这么设计"，规格讲"每个按钮点了之后系统干什么"，两份不合并。
- 裁剪版认知架构图 `pub_plan/认知架构图.md` 验收通过。

### 阶段六：逐模块推进（已完成，2026-09-01 收尾）

7 模块（数据库 → 鉴权 → 前端框架 → 多轮上下文 → embedding+向量知识库 → 统一推理入口 P → 智能体流水线）已全部完成。各模块扩展方向已迁入阶段七台账。

SSE 裁定：SSE 只做任务进度事件推送（推进度不推正文流），规格要求不展示 AI 思考过程、只显示"请等待"与"正在写第 N 节共 M 节"；长任务状态机制由第 11a 项接管。

阶段六推进规则第 4 条（仍生效）：阶段七第 0 步扩展补齐时以大厂企业级标准审视整个项目——具备应对高并发的能力，凡与正式生产标准不符的提出修改。

### 阶段七：毕业

| 顺序 | 模块 | 产出 | 验收标准 |
|---|---|---|---|
| 0 | 扩展补齐 | 各模块生产版（见阶段六推进规则第 4 条） | 每个补齐部分走逐行验收 |
| 1 | 部署 | 部署脚本/流程 | 项目能在目标环境跑起来（本地模拟出版社环境；nginx 等新技术届时以新模块身份登记进场） |
| 2 | 文档 | README | 陌生人能照着跑起来 |
| 3 | 复盘 | 每篇 200 字复盘 | — |
| 4 | 毕业考试 | — | 三问全过 |

**第 0 步扩展补齐台账 v2（生产硬化版）**：预设背景＝请求无限＋语料无限。范围锁死在此表，表外新需求一律先登记再裁定。每个工具必须在本项目里有真岗位，不做孤立 demo。编号＝登记 ID 非执行序，做哪项看"依赖"列。

| 编号 | 组 | 项目 | 在本项目的岗位 | 依赖 | 粗估 |
|---|---|---|---|---|---|
| 0 | ~~已完成~~ | git 建仓（2026-09-03）：本地 git init + 连远程仓库 + 首次推送 + 上游追踪 | — | — | — |
| 1 | ~~已完成~~ | P 加超时/重试（2026-09-03） | — | — | — |
| 2 | ~~已完成~~ | 裸 SQL 换 ORM（SQLAlchemy 2.0）＋Alembic 迁移（2026-09-11 收尾）：10 张新表建好、旧三表在迁移里 drop、迁移基线接上已存在库 | — | — | — |
| 3 | B 欠账 | 完整登录注册（多用户） | 安全硬要求：密码 bcrypt 哈希禁明文，token 过期/刷新/吊销；多用户硬需求：users 表＋业务表加 user_id 归属＋默认行级隔离，可见范围由角色策略决定（归第 27 项） | 第 2 项先做（**硬前置**：第 4 项前端要登录态才能分角色渲染） | 1.5 会话 |
| 4 | B 欠账 | 前端组件库重写（拆 4a~4g） | Element Plus + Vite 工程化 + vue-router（登录/写作/历史）+ Pinia 全局状态；实现与验收规格＝`pub_plan/产品功能设计定稿.md` 第一、二部分 | 第 2、3 项先做；**除 4a 纯脚手架可与 36 并行外，4a 新建表单 submit 及 4b~4g 接数据均须等 36 端点就位** | 见 4a~4g，合计 9.5~11.5 会话 |
| 4a | B 欠账 | Vite 工程初始化＋Element Plus＋Vue Router＋Pinia 基础配置与目录结构＋左侧栏（可收缩＋写作/历史两入口＋「+ 新建」）＋登录/注册页组件＋路由守卫＋axios token 自动携带与 401 跳登录＋「没有正在写的书」状态的新建表单（书名/大致要求/「开始生成大纲」，规格 §2.1） | 工程地基，4b~4g 全挂在它上面 | 第 2、3 项后（Vite 脚手架/登录页/路由守卫/axios 拦截器可与 36 并行；新建表单 submit 须等 36） | 1~1.5 会话 |
| 4b | B 欠账 | AI 显示区：大纲块（钉底、只显示最新份、结构化树编辑器：节点增删改＋调层级调顺序、失焦/Ctrl+S 暂存、自己的版本箭头、可选中加索引块、看/改两态、切版自动对焦、三色标记）＋块内状态提示行（壳，数据由 4g 接） | 规格 §2.2；大纲块是功能最密的一块 | 4a、36 项后 | 2~2.5 会话 |
| 4c | B 欠账 | 固定提示行（程序写死、只在检测到大纲有改动时出现、带「生成初稿」按钮）＋输入框（索引块 chip、左上角 × 取消、大纲/正文块混放、内容变动后失效、生成期间禁用） | 规格 §2.2 第二三截、§2.4 | 4a 后 | 1 会话 |
| 4d | B 欠账 | 右侧栏：只有当前选中节正文（无大纲树）、渲染态/编辑态切换、三色 diff 渲染（绿增/黄改/红小三角不占下一行）、版本箭头、切版自动对焦、「以此版为准继续修改」弹确认舍弃后续、提交弹确认 | 规格 §2.3、§2.7~§2.10 | 4a、36 项后（三色 diff 数据来自 36c 的 GET /diff） | 2~2.5 会话 |
| 4e | B 欠账 | 局部重写交互：左键长按拖动选区 → 松开浮出「添加到会话」→ 生成索引块 → 发送 → 重写结果回填进暂存 | 规格 §2.6 改法三 | 4c、4d 后 | 1~1.5 会话 |
| 4f | B 欠账 | 历史记录页：列表（只显示书名、按书名搜、最近改过排最上）＋详情（左大纲树/右正文默认第一节、底部输入框、提交修改同步更新不新增记录）＋「让 AI 修改」回流写作页 | 规格 §三；与 4b/4d 复用同一套编辑器组件 | 4b、4d 后 | 1.5~2 会话 |
| 4g | B 欠账 | 前端 EventSource 接 SSE 进度事件→4b 建的大纲块状态提示行壳＋失败节「重试本节」 | 规格 §2.2 状态提示、§2.3 失败态 | 4a、4b、28、36f 项后 | 1 会话 |
| 5 | ~~销账~~ | ~~多轮历史截断/摘要~~ 销账（/ask 下线，岗位消失，上下文体量控制归第 36 项） | — | — | — |
| 6 | B 欠账 | embedding 批量调用 | 建库提速 | 无 | 半会话 |
| 7 | B 欠账 | 查重性能优化（建 n-gram→文档倒排索引或 MinHash 候选召回，只比对命中候选不逐篇全库扫。达标：查重延迟随语料规模亚线性增长）；公式渲染移出（规格 §十一 裁定本轮不做，MathJax 重排 DOM 打乱 offset，与局部重写、三色 diff 冲突） | "语料无限"预设下手写全库比对撑不住，须索引化 | 靠近部署时做 | 零散~1 会话 |
| 8 | C 承压 | 多进程服务（uvicorn 多 worker） | 单进程扰不住并发；多 worker 分担＋worker 内异步（第 30 项）配套 | 无 | 1 会话 |
| 9 | C 承压 | nginx 反向代理＋静态资源＋TLS 加密 | 统一入口，全站禁明文传输 | 第 8 项后 | 1 会话 |
| 10 | C 加码 | APISIX 网关（路由/负载均衡/限流） | 网关级粗限流（入口/IP/接口 QPS），与第 13 项应用级细限流分层；与 nginx 职责重叠处开工时裁定 | 第 9 项后 | 1~2 会话 |
| 11a | C 承压 | 长任务最小机制：MySQL `tasks` 表落任务状态＋后台串行执行＋进度写入 `tasks.progress_json`（推送通道归第 28 项、前端消费归第 4g 项）＋`task_id` 断线恢复；失败重试/标败策略 | **硬前置**：整本生成≈上百次模型调用、5~15 分钟，同步 HTTP 必超时。与第 28、4g 项分工焊死——本项产生进度数据、28 提供推送通道、4g 前端消费，三者不重叠不成环 | 第 2 项后；**前置于第 28、36 项** | 1~1.5 会话 |
| 11b | C 承压 | Redis 任务队列＋后台 worker（任务状态落库已由 11a 完成，本项只做队列与 worker 扩容） | 长任务不占接口周期；worker 崩了任务不丢。并发三解法分工：本项队列（架构级）、第 8 多 worker（进程级）、第 30 异步（协程级），互补不替代；量大后队列职能由第 12 项 Kafka 接管 | 第 11a、4 项后 | 1~1.5 会话 |
| 12 | C 加码 | Kafka 消息队列 | 无限任务预设下接管重任务流；Kafka 上线后第 11b 项 Redis 队列职能收敛，Redis 退守缓存（第 13 项） | 第 11b 项后 | 1~2 会话 |
| 13 | C 承压 | Redis 缓存＋限流 | 热点召回缓存；应用级细限流（用户/业务配额），与第 10 项网关级粗限流分层 | 与第 11b 项共用 | 1 会话 |
| 14 | C 承压 | Docker Compose 容器化（healthcheck、restart 崩溃自恢复、mem_limit、MySQL/Redis 数据持久化挂 named volume）；healthcheck 探针调第 19 项 /health 端点 | MySQL/Redis/APISIX/nginx 一把拉起，可部署地基 | 各组件就位后＋第 19 项第二批就位 | 1 会话 |
| 15 | D 数据 | 向量库升级（JSON → FAISS 类索引），含索引持久化（write_index 存盘 / 启动 read_index 加载）＋增量写入（供第 24 项调用）；分库策略＝每教材一独立索引；**索引 schema 绑定 embedding 模型——换模型即触发全量重建** | 百万语料下逐条算 cos 必死；升级成"库"必须能存盘复用 | 无 | 1 会话 |
| 16 | E 规范 | pytest 自动化测试；外部依赖（DashScope API）一律 mock，测试用独立测试库 | 毕业考试排查 Bug 的地基 | 无（鉴权接口部分待第 3 项后补测） | 1.5 会话 |
| 17 | E 规范 | CI 流水线（GitHub Actions：pytest + ruff，两者都过才放行） | 坏代码进不了仓库 | 第 0、16、18 项后 | 半会话 |
| 18 | E 规范 | 代码规范（ruff）＋OpenAPI 文档 | 大厂基本盘 | 无 | 半会话 |
| 19 | E 规范 | 结构化日志＋错误处理＋健康检查＋链路追踪（request_id 贯穿流水线），日志脱敏。**分两批**：第一批＝最小证据链——app.py except 必须同时落日志（终端＋文件），异常带上下文，服务启动时打印关键配置加载结果，rag.py 路径锚 `Path(__file__).parent`；第二批＝健康检查＋request_id＋日志脱敏 | 上线出事能查，且不留泄密后门。**第一批同时是 bug 排查训练的前置** | 第一批：无依赖，前置于 bug 排查训练；第二批：贯穿，与第 25 项衔接 | 第一批半会话；第二批 1 会话 |
| 20 | E 规范 | 生产标准整体审视（并发/配置安全/文件结构），结构问题靠近部署时裁定；认知架构图随台账同步更新 | 全局把关 | 贯穿 | 零散 |
| 21 | F 加码 | K8s（本机 minikube） | 容器编排学习，队尾可砍 | 第 14 项后 | 2~3 会话 |
| 22 | G 安全 | 数据安全加固：前端渲染转义防 XSS、CORS 白名单、数据库最小权限账号、MySQL 定期备份（存本地/出版社内网、不出域） | 不注入、不越权、数据不丢 | 第 3、4 项后 | 1 会话 |
| 23 | G 安全 | 数据出域合规：生产版切断外部云 API，全链路切本地模型（LLM→Ollama≤7B；embedding 同步切本地轻量模型）。三个跨线裁定：① **模型格式转换**——飞桨 LoRA 权重 vs Ollama 只认 GGUF，三选一（合并回基座转 GGUF / 飞桨推理服务 / 出版社特批），与主线②阶段六同步裁定；② **基座型号钉同一个**——主线②在哪个开源基座做 LoRA，本项就得用 Ollama 加载同一基座＋适配器，须是可下载开源型号（如 Qwen2.5-7B-Instruct），不能用开发期 qwen-plus；部署前须用生产基座回归测 prompt/行为；③ **切 embedding 须全量重建向量索引**——旧向量与新模型空间不兼容，衔接第 15、24 项 | 出版社"数据不出域"硬要求 | 卡主线②阶段六交接打包，与挂起项同批插回 | 零散~半会话 |
| 24 | D 数据 | 语料入库产品化：建库脚本升级为管理接口（新教材入库自动切块→向量化→存库，增量更新、按教材分库＝与第 15 项统一；图片素材转述入库见第 31 项） | 加语料不再手跑脚本；向量化复用第 6 项 | 第 6、15 项后 | 1 会话 |
| 25 | H 可观测 | 监控告警（Prometheus＋Grafana）：接口延迟、队列积压、模型调用失败率仪表盘 | 日志是出事后查，监控是事前盯住 | 第 11b、19 项后 | 1 会话 |
| 26 | H 可观测 | 压测演练（wrk/locust）：承压层全就位后模拟高并发 | "请求无限"预设的验收考试 | 第 8~14、30 项后 | 1 会话 |
| 27 | G 安全 | 角色权限（RBAC）：users 表加 role 字段（editor/senior_editor/admin）＋路由级权限校验＋前端按角色藏入口＋敏感操作审计＋数据可见范围策略（普通编辑只看自己的书、高级编辑/管理员看全部） | 与第 3 项分工——第 3 管认证＋默认行级数据归属，本项管角色＋功能级授权＋数据可见范围 | 第 3、4 项后 | 1 会话 |
| 28 | C 承压 | SSE 推送通道：只做后端 SSE 端点（读第 11a 项 `tasks.progress_json` 推给前端）；推进度不推正文流。P 的流式变体与前端 EventSource 消费均不在本项（前者本轮不做，后者归第 4g 项） | SSE＝服务器单向推送，比 WebSocket 轻且对位 | 第 1、11a 项后 | 半会话 |
| 29 | F 加码 | WebSocket 统一实时消息通道：每登录用户一条常驻连接，承载任务进度/完成通知/审计事件广播 | 学习目的进场，熔断时与第 12、21 项同批优先砍 | 第 28、11a 项后 | 半~1 会话 |
| 30 | C 承压 | 异步改造（async/await）：P、RAG 召回、数据库 IO、外部 API 调用等同步阻塞点改异步；流水线内可并行的智能体步骤并发化 | 治本"同步阻塞"——第 8 项多 worker 只是多开进程分担，单 worker 内慢请求仍占死 | 与第 8 项配套 | 2~3 会话 |
| 31 | B 欠账 | 图片素材接 Qwen-VL 转述链：教材图片素材上传后经 P 调 Qwen-VL 转成文字描述，再复用第 24 项入库链向量化进库。开发期走外部 Qwen-VL；生产期图片也属出版社数据、出域合规与第 23 项冲突，届时二次裁定 | 教材含图片素材，图片不进知识库则检索缺图片内容 | 第 6、24 项后 | 1 会话 |
| 32 | B 欠账 | 负面清单 U、重写循环完善：教案生成后按负面清单 U 自检，不合格触发重写循环，直到合格或达重写上限 | 一次生成质量不稳，负面清单 U 是质量闸、重写循环是兜底 | 第 1 项后 | 1 会话 |
| 33 | ~~已完成~~ | 完整初稿生成流程打通（2026-09-04）。产出四处被功能设计取代（选节降级、两端点删除、/ask 下线、签名待改），后端实现归第 36 项 | — | — | — |
| 34 | D 数据 | edit_pairs 语料导出脚本：把 `edit_pairs` 表里三类对照资料导出成主线② D3 微调训练格式 | 跨线焊点实体：主线① 攒原料、主线② 转成品。约定＝表存原料、导出脚本转格式 | 第 2 项后；主线② D3 格式定稿后补齐转换逻辑 | 半~1 会话 |
| 35 | B 欠账 | 全书一致性终审 Agent：整本生成完后跑一遍全书级检查——术语前后是否统一、有没有重复解释同一概念、前面留的伏笔后面有没有呼应 | 补四件套注入覆盖不到的全书级联动 | 第 36 项后；第 32 项后（审出问题走重写循环） | 1~1.5 会话 |
| 36 | B 欠账 | 规格 §九/§十 后端实现（33 项销账接棒；拆 36a~36f——后端版 item4，不许压成 3~4 会话赶工降格）：§九 新端点组＋§十 pipeline 函数＋rag/differ.py＋submit 事务＋意图判定＋整本串行编排 | 不做则第 4 项前端有界面没接口、端到端跑不通 | 第 2、11a 项后；**前置于第 4、35 项** | 见 36a~36f，合计 6~9 会话 |
| 36a | B 欠账 | §九 端点骨架＋books/sections 的 CRUD 与暂存版本链端点 | 建书/列表/详情/暂存/版本链，是 36b~36f 的端点地基 | 第 2、11a 项后 | 1~1.5 会话 |
| 36b | B 欠账 | §十 大纲侧 pipeline 函数（outline_agent 改签名收 title＋requirement、parse_outline 解析节树、align_nodes 标题序列对齐保 node_key、revise_outline_agent、summarize_agent 摘要卡、build_glossary 累积术语表）＋端点 outline/generate、outline/revise | 四件套注入的产出端；node_key 对齐是大纲变更后节一一对应的命门 | 36a 后 | 1.5~2 会话 |
| 36c | B 欠账 | rag/differ.py（diff 唯一实现）＋GET /diff 端点：页面三色标记与语料 diff_ops_json 共用同一份计算结果 | diff 唯一真源；算错则页面显示与语料对不上、微调数据全废 | 第 2 项后 | 0.5~1 会话 |
| 36d | B 欠账 | POST /books/{id}/submit 整本提交事务：一个事务里清暂存＋建版本＋落语料 edit_pairs＋改书 status＋版本链只留最近 5 版裁剪 | 整本提交＝多表事务，裸 SQL 撑不住；语料只在此刻落库 | 36a、36c 后 | 1~1.5 会话 |
| 36e | B 欠账 | POST /intent 意图判定＋classify_intent：指令连同书状态交 LLM 判意图，判成「改大纲会作废正文」先弹确认；书状态上下文体量控制（承接第 5 项销账岗位） | 输入框指令的总路由；不可逆操作判错要加闸 | 36b、11a 后 | 0.5~1 会话 |
| 36f | B 欠账 | POST /books/{id}/draft/generate 整本串行编排 generate_book（18 节×审校串行＋四件套注入＋进度上报写 tasks.progress_json）＋POST sections/{id}/generate 补单节＋POST sections/{id}/rewrite 局部重写 rewrite_passage_agent | 规格 §7.2 整本对用户一次、对模型一节一节串行；进度上报对接 11a/28/4g | 36b、36e、11a 项后 | 1.5~2 会话 |
| 挂起 | — | P 接主线②适配器 | 卡主线②微调产出，就绪后插回；基座型号须与主线②微调基座一致（见第 23 项） | — | — |
| 挂起 | — | 微调任务集成：前端提交入口→任务队列（复用第 11a/11b 项）→训练脚本（主线②产出）→适配器产物→P 加载 | 卡主线②阶段五/六，与适配器插回同批 | 主线②就绪＋第 11a/11b 项完成 | — |

**P1 待办（不占排期、不计入工期）**：

| 编号 | 待办 | 什么时候该做 |
|---|---|---|
| P1-1 | 节级前置依赖标注（显式标「第 5 节依赖第 2 节」） | 单本节数超过 50 |
| P1-2 | 按章并行、章内串行（现在是纯串行） | 单本生成时间成为主要痛点 |
| P1-3 | 长任务取消 | 用户开始抱怨停不下来 |
| P1-4 | 公式渲染与字符 offset 共存（MathJax 重排 DOM 打乱 offset，与局部重写、三色 diff 冲突） | 教材必须含公式时 |

执行原则：全部走 B 档；影响正确性当场修，性能/架构按台账排期；每项完成后遗留区销账一行；基础设施一律 Docker 拉起；依赖清单第 2 项开工前补齐。

时间预算：第 0 步约 **50~63 次会话**（≈ 17~21 周，含第 30 项异步改造 2~3 会话）。之后部署/文档/复盘/毕业另留 ≥ 2 周，阶段七全程约 **19~23 周**。

熔断条款：外部硬节点＝主线②开学考核（时间未定）；时间一定，备考期本线暂停或压缩，工期顺延；算不平则按序砍：队尾（第 21 项）→加码项（第 12、10、29 项）→第 35 项与 P1 待办，B 组欠账与 C/D/G 承压安全层不动。**不可砍**：第 4a~4g、11a、34、36a~36f（功能最小集＋跨线焊点）。

排除裁定：配置中心/服务注册发现/灰度发布等微服务组件不进表（单体无岗位，拆微服务之日再登记）。服务器成本不属本项目预算：生产硬件由出版社采购，学习阶段全部开源免费、Docker 本机拉起。

**AI 使用三档制**：

| 档位 | AI 可以做什么 | 前提条件 |
|---|---|---|
| A 档（教练） | 埋雷、翻译报错、当橡皮鸭 | 默认档，任何时候可用 |
| B 档（扩展） | 把手写通过的最小版扩展成生产版 | 该模块最小版已手写跑通并通过口述验收 |
| C 档（生成） | 直接生成样板代码 | 仅限已能说出该类代码结构的模块，生成后逐行验收 |

红线：**任何档位下，AI 产出的代码没经过你"为什么这么写"的逐行解释，不许合入项目。**

**协作代码验收**——项目与他人协作时，别人写的代码按以下规则处理：
1. 别人拿进项目的代码 = C 档生成代码同等待遇：你说不清"为什么这么写"就不许合入。
2. 分级要求：主线核心模块（阶段六表内 7 个）每行都要说得清——毕业抽问范围；外围模块至少说清顶层——干什么的、和哪个接口相接。
3. 分工先保练习：我正在练的模块必须我写；协作者先写了的，二选一——换练习模块，或对其代码做逐行验收把它"变成"我的。

---

## 2. 时间管理

**开学版**：按课表（d:\Learning\课表.md，唯一事实源）核定。学习日＝周一~周五＋周日（周六休息零额度；周日晚百年党史为水课，零投入不计），一天一条线与深度学习线轮转，每次会话 1h 保底、2h 理想 → 本线每周约 3 次会话（3~6h）。节假日按届时实际放假安排：假期不强制学习，轮转顺延不重置。若算不平→深度学习线降级为只应付社团作业。

会话组成（时长按实际会话时长等比压缩）：

| 环节 | 内容 |
|---|---|
| 开场 | 默写复盘：说不出来的就是今天第一任务 |
| 主体 | 手写 + 埋雷修雷；资料只查马上要用的那一小块 |
| 收尾 | 口述今天学了什么，教练对照当天收件箱条目检查 |

原则：
- 时间花在"卡住-想通"上，不是行数上。10 行真懂 > 200 行抄写。
- 卡住 30 分钟必须停手，写下"期望/实际/猜想"三行再求助。
- **周末加餐＝bug 排查训练**：每周固定 1~2 次、每次 ≤1h，不额外占两条主线时间额度。规则由 bug 排查师守则（`d:\Learning\.qoder\rules\bug_guidelines.md`）管。开工前置＝台账第 19 项第一批已落地 ＋ git 工作区干净。

---

## 3. 环境与硬件约束

- **后端启动命令（焊死）**：永远从项目根（d:\Learning\pub_of_agent）执行
  `d:/Learning/pub_of_agent/.venv/Scripts/python.exe -m uvicorn rag.app:app`
  禁止 `python rag/app.py` 直接跑（会撞 `'rag' is not a package`）。
- **部署目标：本地化部署＝部署在出版社提供的目标环境（数据不出域），不是跑在自己笔记本上**。学习阶段跑本机小规格 + API；生产硬件由出版社采购。
- 硬件上限：显存 8G → Ollama 只跑 ≤7B 的 Q4 量化模型（约 5G）；embedding 用轻量版（≤1G）。超规格不考虑。
- 上半场不碰 GPU（mock LLM + TF-IDF 纯 CPU）；Ollama 到下半场接真实 LLM 时才装。
- 教材智能体原型阶段：LLM 走 DashScope API（账号已有/可申请），不占本地显存；本地模型到生产阶段再引入。
- pip 环境：包源＝pypi.org 默认，未配置镜像；全局缓存目录＝D:\pip-cache（与主线②共用）。已知坑：开着代理软件访问 pypi 可能被分流拦断报 SSL 错误，先关代理重试；仍不通用阿里云镜像兜底：`pip install 包名 -i https://mirrors.aliyun.com/pypi/simple/`。
- 微调（fine-tuning）归主线2承担，主线1不重复学。

---

## 5. 外部项目与学习主线的映射

**总纲：两条学习主线——①全栈+智能体（本方案）；②深度学习（飞桨）。主线上每一行都要说得清；其余事项一律 AI 代写应付。**

| 事项 | 定位 | 投入原则 |
|---|---|---|
| 教材智能体学习项目（本目录 `pub_of_agent`） | 主线①毕业作品 | 按阶段七台账继续推进，慢做细做；知识库搭建＝RAG 升级素材；本地部署＝§3 的 Ollama 部分；适配器微调＝主线②内容，学一次两处用 |
| 出版社原型工作任务 | 协作型外部项目（外部已有全栈原型 publisher-agent-release 部署上云供出版社试用） | 工作代码一律进独立目录，不进 `d:\Learning`；AI 生成须过三档制 B/C 档验收，绝不似懂非懂交付；协作代码验收规则同等适用。外部原型缺口（无数据库/无鉴权/BM25 检索/无 LoRA）恰为主线①②在练内容，适配器产物可经 P 插入其原型交付 |
| 飞桨/深度学习 | 主线② | 独立项目、独立守则+方案，不与主线①混账 |
| 绘画智能体 | 封存 | 需求未定，零投入 |
| 大创 AGENT | 封存 | 无人催，零投入 |
| 万方 | 最低投入维护 | AI 代写应付，不占学习时长、不算学习成果 |
| 课程/实验报告 | 固定成本 | 固定时段压缩处理，不许侵蚀学习时段 |
| 社团活动 | 事件驱动 | 来了应对，不预留整块时间 |

**风险规则（任务来得比学得早时）**，只有两条路：
1. 先学后做：能纳入主线的就当练习素材；
2. 走三档制 B/C 档：AI 写＋你逐行验收。

绝不允许"似懂非懂接手"。AI 应付项的底线：交出去的项目至少能说出顶层是干什么的。

---

## 6. 踩坑清单

> 用途：知识全文进收件箱→归档飞书后，教练在场内就够不着了；本节只存大坑的"一行永久副本"，每次新会话开场和遗留问题区一起查。
> 格式：【日期】症状 ｜ 根因一句话（全文：飞书文档标题）。

- 【2026-08-18】uvicorn 从 rag/ 目录启动 → `'rag' is not a package` ｜根因：模块身份随启动位置变（已焊进 §3 启动命令）

进出规则：
- 什么才进：① 卡了 30 分钟以上；② 概念反直觉、容易再踩。普通笔误不进。
- 什么时候进：周末归档收件箱时，教练对每条问一句"要不要升进踩坑清单"，本人拍板。
- 什么时候出：同一坑换形式重埋过、本人修对了，划掉。

---

## 7. 遗留问题（教练登记区）

> 记录"验收没答上的追问"和"阶段遗留事项"，每次新会话开场先查这里。
> 只记任务和问题，不记知识点（知识点进收件箱）。
> 格式：完成的只留一行结论（完成时间 + 仍生效的决定）。


已完成：
- ✅ 阶段二/三/四验收全部通过（2026-08-18 ~ 08-21）。
- ✅ 阶段五认知架构图二轮验收通过（2026-08-21）。
- ✅ 模块 1 数据库（2026-08-24）。
- ✅ 模块 2 鉴权（2026-08-24）。
- ✅ 模块 3 前端框架（2026-08-25）。仍生效：只做 Vue CDN 最小重写，组件库与生产版重写留阶段七。
- ✅ 模块 4 多轮上下文（2026-08-26）。仍生效：滚雪球膨胀按最小版认账不修，靠 fetch_history(5) 兜底。
- ✅ 模块 5 embedding + 向量知识库（2026-08-28）。仍生效：embedding 走 DashScope text-embedding-v4；向量库＝JSON 文件，不上向量数据库；key 走 .env；批量调用留扩展。
- ✅ 模块 6 统一推理入口 P（2026-08-28）。仍生效：所有生成文本只走 LLM()，走 DashScope OpenAI 兼容（qwen-plus）；多轮历史以消息对进 messages；取历史留在 app.py，P 内部不读库；超时/重试、换 SDK、接适配器留阶段七。
- ✅ 模块 7 智能体流水线（2026-09-01）。仍生效：基座 qwen-plus 经 P 走通，适配器就绪后切换；查重用 n-gram 数重叠，底本＝全库原始语料；召回与存库归编排层，写作智能体不做检索；初稿禁公式（留阶段七前端还）；语料单薄致初稿空泛，靠语料铺开解决。

- 【外部原型】外部出版社侧原型已部署上云。学习与工作目录分开（见 §5）；主线① `edit_pairs` 表替代外部数据依赖（台账第 34 项）。
- 【API key 配置】裁定已进教练守则。
- ✅ 台账第 0 项 git 建仓（2026-09-03）。仍生效：.gitignore 挡密钥/环境/缓存/备份/生成物；源码/文档/语料源/规则配置进版本库；远程仓库 https://github.com/Zack673-cpu/pub_of_agent.git。
- ✅ 台账第 1 项 P 加超时/重试（2026-09-03）。仍生效：P 函数和 get_embedding 都加了 timeout=30、retry 3 次、backoff、错误处理。
- ✅ 台账第 33 项 初稿生成流程打通（2026-09-04）。销账补记：产出四处被功能设计取代——选节降级（整本一次生成、选节保留为补生成单节）、POST /outline 和 /generate 两端点删除（被 §九 端点组取代）、/ask 自由问答下线、generate_outline 签名待改（收 title＋requirement）。
- ✅ 台账第 19 项第一批 最小证据链（2026-09-04）。仍生效：app.py except 加 logger.error + exc_info=True（终端＋文件双写）、启动时打印配置检查、错误 detail 透传、rag.py 路径锚 `Path(__file__).parent`。
- ✅ 产品功能设计定稿（2026-09-06）。交付规格＝`pub_plan/产品功能设计定稿.md`，台账已全部同步（第 2/3/4/5/7/11/27/28/33 项受影响，新增第 34/35/36 项）。
- ✅ ORM 10 张表 4 处问题修复 + relationship 补齐（2026-09-10）。文件拆分启动（backend/）。

- ✅ 文件拆分收尾（2026-09-11）。五步全部完成：models.py 去重复 Base、database.py 写 engine/SessionLocal/get_session、rag/db.py 去 ORM 定义；database.py import 定为 `from backend.models import Base`（顶层包名，配套 `python -m backend.database` 验证，不用直跑）；logging 保留且 `FileHandler` 拼写已修；rag/db.py 的 password 已处理。
- ✅ 台账第 2 项 裸 SQL 换 ORM + Alembic 迁移（2026-09-11）。仍生效：Alembic 目录在项目根（不在 backend 里）；env.py 复用 database.py 的 engine（DB_CONNECTION 从 .env 读，alembic.ini 不写死连接串）；初始迁移 8d974c782604 建 10 张新表 + drop 旧三表（outlines/drafts/conversations），基线接上已存在库；downgrade 保留删 10 张新表（反序），不重建旧三表（旧表永久废弃）。
- ⏳ 台账第 3 项 鉴权 refresh 子块已完成并验收通过（2026-09-14）：`rag/auth.py` 新增 `create_refresh_token_plain`（secrets.token_urlsafe(32)）与 `hash_refresh_token`（sha256.hexdigest，64位hex）；`rag/app.py` 新增 `POST /refresh`（查 row→三校验 无/吊销/过期→轮换：旧revoked、新明文+哈希、继承 row.user_id、expires_at=now+7天、发新 access、commit）。**沿用既有约定**：存 refresh token 哈希不存明文；get_session 走 backend/database.py 的依赖注入（Depends）；过期时间存 UTC、读出裸时间用 .replace(tzinfo=timezone.utc) 对齐比较；row 判 None 须在碰任何属性前。**下一块待办：POST /register + POST /login**，核心风险点＝用户名查重（不能硬让 MySQL 唯一约束异常抛给用户；思考查重与插入间如何防并发重复）。开工时先想清查重方案再动手。
