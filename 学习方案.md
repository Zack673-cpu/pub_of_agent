# 学习方案（主线①：全栈 + 智能体）

> 本文件管"学什么、什么顺序、怎么算完成"；怎么学见 `.qoder/rules/coding-guidelines.md`（教练守则）。


---


## 0. 总目标

**独立产出完整项目**——功能完整、结构规范、可部署、可持续迭代。

核心标准：**每一行代码（无论谁写的）都能说出为什么存在。**
手写是刻意练习手段，不是终点；练成之后你负责架构决策和验收，AI 可以帮你堆量。

毕业考试（三问全过才算毕业）：
1. 有一个拿得出手的完整项目（可部署、有真实功能、有文档）。
2. 任意模块随机抽问"这段为什么这么写"，答得上来。
3. 能独立排查生产级 Bug（AI 只当橡皮鸭，不给答案）。训练机制＝每周固定 1~2 次 bug 排查训练（时段见 §2 周末加餐，规则见 bug 排查师守则）；硬前置＝台账第 19 项第一批「最小证据链」必须先落地，否则项目本身吐不出证据，练的是猜不是查。


---


## 1. 整体路线

```
上半场（刻意练习，全手写，AI 只当教练）
   零：热身（3 天封顶）
   一~四：压舱石三件套 —— 手写 RAG → 裸 API → 丑前端 → 删掉默写重写

下半场（真实项目，你主导 + AI 按三档制辅助）
   五：选定完整项目，画架构图
   六：逐模块推进（数据库→鉴权→前端框架→多轮→embedding→推理入口→智能体流水线，见阶段六）
   七：扩展补齐 + 部署 + 文档 + 复盘，毕业
```


### 阶段零~四：热身 + 上半场压舱石训练营（✅ 已完成）

看课（B站 RAG 入门，带过滤器）+ 知识闸门（Python 基础 / RAG 管道六步）→ 30 行手写 RAG（`rag/rag.py`）→ FastAPI 裸接口（`POST /ask`）→ 极简前端（`index.html`）→ 删掉重写默写版。关键取舍：先 RAG 后 Agent、全栈从后端开始、禁区（厚框架 / 多智能体 / 向量库调优）阶段一~四不碰。


---


### 下半场：完整项目

#### 阶段五：选定项目 + 架构图（✅ 已完成，2026-08-21）

- **完整项目＝教材智能体（出版社智能体）**（✅ 定 2026-08-19）。
- **语言＝Python**（与上半场成果、主线②飞桨同生态；本地小规模部署不需换 Go）。
- 项目方案 `pub_plan/教材智能体方案设计.md` **仅设计参考、不是交付规格**，按学习路线裁剪分阶段做，不为对齐蓝图而“似懂非懂接手”；方案不含微调，微调由主线②承担。
- **交付规格＝`pub_plan/产品功能设计定稿.md`（✅ 定 2026-09-06）**：前端与后端照它实现、照它逐条验收。与蓝图分工——蓝图讲“是什么、为什么这么设计”，规格讲“每个按钮点了之后系统干什么”，两份不合并。规格定稿后认知架构图需同步更新（改动清单见规格 §十二，按台账第 20 项归本人自改）。
- 裁剪版认知架构图 `pub_plan/认知架构图.md` 二轮验收通过，为总验收标准（已学讲内部细节、未学答盒子三问）。


#### 阶段六：逐模块推进（✅ 已完成，2026-09-01 收尾）

7 模块（数据库 → 鉴权 → 前端框架 → 多轮上下文 → embedding+向量知识库 → 统一推理入口 P → 智能体流水线）已全部走“最小手写 → 口述 → AI 扩展（B 档）→ 逐行验收”完成（各模块仍生效决定见 §7）。各模块扩展方向已全部迁入阶段七台账：ORM→第 2、登录注册→第 3、框架重写→第 4、截断摘要→第 5、图片 Qwen-VL 转述→第 31、超时重试→第 1、负面清单 U/重写循环→第 32、批量 embedding→第 6、接主线②适配器→挂起项。

SSE 裁定（2026-09-02 激活，**✅ 2026-09-06 用途变更**）：SSE 从“大纲逐字流式显示”改为**任务进度事件推送**——功能规格要求不展示 AI 思考过程、只显示“请等待”与“正在写第 N 节共 M 节”，故不推正文流只推进度；长任务状态机制由第 11a 项接管（落地见台账第 28、11a 项）。

**阶段六推进规则第 4 条（企业级标准，仍生效）**：阶段七第 0 步扩展补齐时以大厂企业级标准审视整个项目——具备应对高并发的能力，凡与正式生产标准不符的提出修改，确保部署后（除硬件环境限制外）可直接当产品上市方向靠拢、方方面面尽可能好。


#### 阶段七：毕业

| 顺序 | 模块 | 产出 | 验收标准 |
|---|---|---|---|
| 0 | 扩展补齐 | 各模块生产版（见阶段六推进规则第 4 条） | 每个补齐部分走逐行验收 |
| 1 | 部署 | 部署脚本/流程 | 项目能在目标环境跑起来（本地模拟出版社环境；nginx 等新技术届时以新模块身份登记进场） |
| 2 | 文档 | README | 陌生人能照着跑起来 |
| 3 | 复盘 | 每篇 200 字复盘 | — |
| 4 | 毕业考试 | — | 三问全过：有完整项目（可部署、有真实功能、有文档）；任意抽问"这行为什么这么写"答得上来；能独立排查生产级 Bug |

**第 0 步扩展补齐台账 v2（生产硬化版，✅ 定 2026-09-01）**：预设背景＝请求无限＋语料无限，项目在此前提下要尽可能扰得住。范围锁死在此表，表外新需求一律先登记再裁定，不许边补边长。每个工具必须在本项目里有真岗位，不做孤立 demo（新工具进场按教练守则"新阶段开场"走新词盘点）。编号＝登记 ID 非执行序，做哪项看"依赖"列。

| 编号 | 组 | 项目 | 在本项目的岗位 | 依赖 | 粗估 |
|---|---|---|---|---|---|
| 0 | A 基础 | git 建仓：本地 git init + 连远程仓库（GitHub）+ 首次推送 + 上游追踪（.gitignore 已有，按需补充） | 版本安全网＝必须有异地远程备份；只在本地一块硬盘上的 git 不算安全网，先于一切扩展 | 无 | 半会话内 |
| 1 | B 欠账 | P 加超时/重试 | 网络抗抖，全项目受益 | 无 | 半会话 |
| 2 | B 欠账 | 裸 SQL 换 ORM（SQLAlchemy）＋Alembic 迁移；须为现有三张表建 Alembic 迁移基线（alembic stamp 标基线或初始迁移含现有表），否则迁移历史接不上已存在的库。**开工前先做「数据模型定稿复核」（✅ 增 2026-09-06）**：照规格 §八 建 10 张新表（users / books / outline_versions / outline_staging / sections / draft_versions / staging / edit_pairs / rewrite_requests / tasks）＋conversations 扩列（**conversations 岗位待定，建表前先裁：定岗或 drop，见规格 §八；staging 只管正文暂存、大纲暂存归 outline_staging，规格已修**），旧 outlines / drafts 两表在迁移里 drop | 改表结构不再手改库；做了迁移就得把现有库纳进迁移体系才算达标；**表从 3 张涨到 10 张后裸 SQL 撑不住多表事务**（整本提交＝一个事务里清暂存＋建版本＋落语料＋改书状态）；ORM 用 SQLAlchemy 2.0，数据访问统一走 session，为第 30 项异步改造预留换 async 引擎的余地 | 无（**升为硬前置**：第 3、4、11a 项全靠它） | 2~2.5 会话 |
| 3 | B 欠账 | 完整登录注册（多用户） | 无 token 调接口被拒；安全硬要求：密码 bcrypt 哈希禁明文，token 过期/刷新/吊销；多用户硬要求：users 表＋业务表加 user_id 归属＋**默认行级隔离，可见范围由角色策略决定**（✅ 改 2026-09-06：原“各编辑的对话/稿件互不可见、取历史只取本人”作废——规格 §四 定了普通编辑只看自己的书、高级编辑/管理员看全部人的书，故隔离是默认值不是死规则，具体范围归第 27 项角色策略裁） | 第 2 项先做（**升为硬前置**：第 4 项前端要登录态才能分角色渲染） | 1.5 会话 |
| 4 | B 欠账 | 前端组件库重写（**✅ 拆 4a~4g，2026-09-06**） | 多组件复杂状态，框架回本；组件库定 Element Plus（Vue3 生态主流，靠组件拼装＋布局模板出观感，不手写 CSS）；多用户硬需求：多页路由（vue-router：登录/写作/历史；**微调页归挂起项「微调任务集成」、本项不建**）＋全局状态（Pinia：登录态/角色/当前书跨组件共享）；**构建方式已裁定＝Vite 工程化**（原“开工时裁定”已裁：CDN 单文件扛不住本版组件量与状态复杂度）；**实现与验收规格＝`pub_plan/产品功能设计定稿.md` 第一、二部分** | 第 2、3 项先做；**除 4a 纯脚手架可与第 36 项并行外，4a 新建表单 submit 及 4b~4g 接数据均须等第 36 项端点就位**（规格 §九） | 见 4a~4g，**合计 9.5~11.5 会话** |
| 4a | B 欠账 | Vite 工程初始化＋Element Plus＋Vue Router＋Pinia 基础配置与目录结构＋左侧栏（可收缩＋写作/历史两入口＋「+ 新建」）＋登录/注册页组件＋路由守卫＋axios token 自动携带与 401 跳登录＋「没有正在写的书」状态的新建表单（书名/大致要求/「开始生成大纲」，规格 §2.1） | 工程地基，4b~4g 全挂在它上面（规格 §一）；登录页与新建表单是规格未展开但第 3 项后端的必备配套前端，由本项实现 | 第 2、3 项后（**Vite 脚手架/登录页/路由守卫/axios 拦截器可与 36 并行；新建表单 submit 调 POST /books 须等 36**） | 1~1.5 会话 |
| 4b | B 欠账 | AI 显示区：大纲块（钉底、只显示最新一份、结构化树编辑器：节点增删改＋调层级调顺序、失焦/Ctrl+S 暂存、自己的版本箭头、可选中加索引块、看/改两态、切版自动对焦、三色标记）＋块内状态提示行（壳，数据由 4g 接） | 规格 §2.2；大纲块是功能最密的一块 | 4a、36 项后（大纲树数据来自 §九 端点） | 2~2.5 会话 |
| 4c | B 欠账 | 固定提示行（程序写死、**只在检测到大纲有改动时出现**、带「生成初稿」按钮）＋输入框（索引块 chip、左上角 × 取消、大纲/正文块混放、内容变动后失效、生成期间禁用） | 规格 §2.2 第二三截、§2.4 | 4a 后 | 1 会话 |
| 4d | B 欠账 | 右侧栏：只有当前选中节正文（无大纲树）、渲染态/编辑态切换、三色 diff 渲染（绿增/黄改/红小三角不占下一行）、版本箭头、切版自动对焦、「以此版为准继续修改」弹确认舍弃后续、提交弹确认 | 规格 §2.3、§2.7~§2.10 | 4a、36 项后（三色 diff 数据来自 36c 的 GET /diff） | 2~2.5 会话 |
| 4e | B 欠账 | 局部重写交互：左键长按拖动选区 → 松开浮出「添加到会话」→ 生成索引块 → 发送 → 重写结果回填进暂存 | 规格 §2.6 改法三 | 4c、4d 后 | 1~1.5 会话 |
| 4f | B 欠账 | 历史记录页：列表（只显示书名、按书名搜、最近改过排最上）＋详情（左大纲树/右正文默认第一节、底部输入框、提交修改同步更新不新增记录）＋「让 AI 修改」回流写作页 | 规格 §三；与 4b/4d 复用同一套编辑器组件 | 4b、4d 后 | 1.5~2 会话 |
| 4g | B 欠账 | 前端 EventSource 接 SSE 进度事件→4b 建的大纲块状态提示行壳（只接数据，不另建壳）＋失败节「重试本节」 | 规格 §2.2 状态提示、§2.3 失败态 | 4a、4b、28、36f 项后（进度数据由 36f 写入 tasks.progress_json） | 1 会话 |
| 5 | B 欠账 | ~~多轮历史截断/摘要~~ **✅ 销账 2026-09-06**：/ask 下线（规格 §六）＋界面不留对话记录，多轮对话历史链路随模块 4 fetch_history 删除，本项岗位消失；新流程上下文体量控制归第 36 项 | 岗位消失，不占排期 | — | — |
| 6 | B 欠账 | embedding 批量调用 | 建库提速 | 无 | 半会话 |
| 7 | B 欠账 | 查重性能优化（现全库逐篇算 n-gram 重叠，百万语料必慢；优化＝建 n-gram→文档 倒排索引或 MinHash 候选召回，只比对命中候选不逐篇全库扫。达标：查重延迟随语料规模亚线性增长，百万语料下单篇查重秒级返回）；**公式渲染移出（✅ 改 2026-09-06）**——规格 §十一 裁定 8 本轮不做（MathJax 重排 DOM 打乱 offset，与局部重写、三色 diff 冲突），公式与 offset 共存见 P1-4 | 查重性能＝“语料无限”预设下手写全库比对撑不住，须索引化 | 靠近部署时做，与第 15 项索引思路对照 | 零散~1 会话 |
| 8 | C 承压 | 多进程服务（uvicorn 多 worker） | 单进程扰不住并发，多开 worker 分担；但每个 worker 内同步阻塞仍在，治本靠第 30 项异步改造，两者配套（多 worker ＋ worker 内异步） | 无 | 1 会话 |
| 9 | C 承压 | nginx 反向代理＋静态资源＋TLS 加密 | 统一入口，全站禁明文传输（内网用自签证书） | 第 8 项后 | 1 会话 |
| 10 | C 加码 | APISIX 网关（路由/负载均衡/限流） | 网关分发；限流＝网关级粗限流（入口/IP/接口 QPS 总量），与第 13 项 Redis 应用级细限流分层不重复；与 nginx 职责重叠处开工时裁定合并或共存 | 第 9 项后 | 1~2 会话 |
| 11a | C 承压 | **长任务最小机制（✅ 从第 11 项拆出并提前，2026-09-06）**：MySQL `tasks` 表落任务状态＋后台串行执行＋进度写入 `tasks.progress_json`（推送通道归第 28 项、前端消费归第 4g 项）＋`task_id` 断线恢复；失败重试/标败策略 | **硬前置**：整本生成＝18 节 × 每节最多 5 轮审校 ≈ 上百次模型调用、实测 5~15 分钟，同步 HTTP 必超时，没它第 4 项前端根本没法用；契合规格「不展示思考过程、只显示请等待」。**与第 28、4g 项分工焊死——本项产生进度数据、第 28 项提供后端推送通道、第 4g 项前端消费，三者不重叠不成环**；本项后台执行框架先就位（dummy 任务跑通即可），业务编排 generate_book 由第 36 项实现、进度写入约定两项对接 | 第 2 项后（任务表走 ORM）；**前置于第 28、36 项** | 1~1.5 会话 |
| 11b | C 承压 | Redis 任务队列＋后台 worker（原第 11 项剩余部分；任务状态落库已由 11a 完成，本项只做队列与 worker 扩容） | 流水线长任务不占接口，焊"长任务用轮询"老决定（原「轮询」部分已由 11a 的 SSE 进度推送取代）；worker 崩了任务不丢、失败有交代。并发三解法分工：本项队列＝长任务移出请求周期（架构级）、第 8 多 worker＝多进程分担（进程级）、第 30 异步＝单 worker 内 IO 不阻塞（协程级），三者互补不替代；量大后队列职能由第 12 项 Kafka 接管 | 第 11a、4 项后（队列与 worker 扩容建在 11a 的任务表之上） | 1~1.5 会话 |
| 12 | C 加码 | Kafka 消息队列 | 无限任务预设下接管重任务流：Kafka 上线后第 11b 项 Redis 队列职能收敛，Redis 退守缓存（第 13 项），不搞两套队列并存 | 第 11b 项后 | 1~2 会话 |
| 13 | C 承压 | Redis 缓存＋限流 | 热点召回缓存；限流＝应用级细限流（用户/业务配额，如每编辑每分钟生成次数），与第 10 项网关级粗限流分层，挡恶意刷接口 | 与第 11b 项共用 | 1 会话 |
| 14 | C 承压 | Docker Compose 容器化，含生产级编排细节（healthcheck 健康检查、restart 崩溃自恢复、mem_limit 内存限、MySQL/Redis 数据持久化挂 named volume——容器重建数据不丢）；healthcheck 探针调第 19 项应用 /health 端点，不各做各的 | MySQL/Redis/APISIX/nginx 一把拉起，可部署地基 | 各组件就位后＋第 19 项第二批（/health 端点）就位 | 1 会话 |
| 15 | D 数据 | 向量库升级（JSON → FAISS 类索引），含索引持久化（write_index 存盘 / 启动 read_index 加载，不随进程重启重建）＋增量写入（供第 24 项语料入库调用）；分库策略＝每教材一独立索引，检索按当前教材路由到对应索引（教材间语料隔离、按教材增量重建、检索空间小），跨教材检索需求出现再加聚合层；**索引 schema 绑定 embedding 模型——换 embedding 模型（如第 23 项切本地）即触发全量重建、不兼容旧向量** | 百万语料下逐条算 cos 必死；升级成“库”就必须能存盘复用，否则每次重启重建百万向量不可接受 | 无 | 1 会话 |
| 16 | E 规范 | pytest 自动化测试（查重/检索/鉴权等核心逻辑与接口）；外部依赖（DashScope API）一律 mock 不打真接口，测试用独立测试库不碰真实数据 | 毕业考试排查 Bug 的地基 | 无（鉴权接口部分待第 3 项后补测） | 1.5 会话 |
| 17 | E 规范 | CI 流水线（GitHub Actions：推代码自动跑 pytest 测试 + ruff 静态检查，两者都过才放行） | 坏代码进不了仓库＝测试不过或风格/静态检查不过都拦下；只跑测试则烂风格代码照样进仓，不达岗位标准 | 第 0、16、18 项后（ruff 见第 18 项） | 半会话 |
| 18 | E 规范 | 代码规范（ruff）＋OpenAPI 文档 | 大厂基本盘 | 无 | 半会话 |
| 19 | E 规范 | 结构化日志＋错误处理＋健康检查＋链路追踪（request_id 贯穿流水线），日志脱敏（用户提问/token 不落盘）。**分两批（✅ 拆 2026-09-03）**：第一批＝最小证据链——app.py 的 `except Exception` 除转 HTTPException 外必须同时落日志（终端＋文件），异常带上下文（哪一步、什么输入），错误 detail 可读不含糊，服务启动时打印关键配置加载结果（.env 是否读到、key 是否为空、向量库/语料路径是否存在），`rag.py` 第 14/16 行相对路径改 `Path(__file__).parent` 锚定；第二批＝健康检查＋request_id 链路追踪＋日志脱敏 | 上线出事能查，且不留泄密后门；多智能体长链路一次请求能追到底。**第一批同时是 bug 排查训练的前置**：现状 app.py 把异常转成 HTTPException(500) 后 uvicorn 视为正常业务响应、不打堆栈，后端终端只有一行访问日志，查不到根因；且相对路径导致换目录启动就 FileNotFoundError，会伪装成 api_key 问题的假线索 | 第一批：无依赖，前置于 bug 排查训练（毕业考试第 3 问），本周末前完成；第二批：贯穿，与第 25 项监控埋点衔接 | 第一批半会话；第二批 1 会话 |
| 20 | E 规范 | 生产标准整体审视（并发/配置安全/文件结构；异步改造已单列第 30 项），结构问题靠近部署时裁定；认知架构图随台账同步更新（本人自改，已学讲到内部细节、未学盒子三问） | 全局把关 | 贯穿 | 零散 |
| 21 | F 加码 | K8s（本机 minikube） | 容器编排学习，队尾可砍 | 第 14 项后 | 2~3 会话 |
| 22 | G 安全 | 数据安全加固：前端渲染转义防 XSS、CORS 白名单、数据库最小权限账号、MySQL 定期备份（备份存本地/出版社内网、不出域，呼应第 23 项） | 不注入、不越权、数据不丢 | 第 3、4 项后 | 1 会话 |
| 23 | G 安全 | 数据出域合规：生产版切断外部云 API，全链路切本地模型（LLM 推理由 Ollama 承载；embedding 同步切本地轻量模型——text-embedding-v4 也属外部 API，不切则语料文本照样出域、岗位不达标；均见 §3 硬件约束）。**模型格式转换通道待裁定（✅ 增 2026-09-06）**：主线②微调产物是飞桨 LoRA 权重、Ollama 只认 GGUF，两者没有直接通路——「合并回基座转 GGUF / 改用飞桨推理服务 / 出版社特批」三选一，与主线②阶段六模块 2 同步裁定，不裁则本项做完也切不了本地。**基座型号须与主线②钉同一个（✅ 增 2026-09-06）**：三选一只解决「格式怎么转」，不解决「两线基座是不是同一个型号」——主线②在哪个开源基座上做 LoRA，本项生产版就得用 Ollama 加载同一个基座＋适配器，否则适配器挂错基座、白训；主线② D3「模型阶段五再定」与本项「≤7B」须同步落到同一可下载开源型号（如 Qwen2.5-7B-Instruct），不能用开发期的 qwen-plus（云、闭源、不可本地微调）。**开发期 qwen-plus ≠ 生产期本地基座，部署前须用生产基座把 prompt/行为回归测一遍**。**切 embedding 模型须全量重建向量索引（✅ 增 2026-09-06）**：text-embedding-v4 向量与本地模型向量空间不兼容，切换后旧向量全废、整个语料库须重新向量化重建索引（衔接第 15、24 项），漏了则检索拿垃圾结果或维度报错 | 出版社"数据不出域"硬要求，靠主线②适配器+本地部署兑现 | 卡主线②阶段六交接打包（含模型格式转换通道裁定），与挂起项同批插回 | 零散~半会话（含本地 embedding 选型验证） |
| 24 | D 数据 | 语料入库产品化：建库脚本升级为管理接口（新教材入库自动切块→向量化→存库，增量更新、按教材分库＝每教材一独立索引，与第 15 项统一；图片素材转述入库见第 31 项） | 语料无限预设下，加语料不再手跑脚本；向量化环节复用第 6 项批量 embedding | 第 6、15 项后 | 1 会话 |
| 25 | H 可观测 | 监控告警（Prometheus＋Grafana）：接口延迟、队列积压、模型调用失败率仪表盘；指标由第 19 项埋点或新增 /metrics 端点暴露，Prometheus 抓取 | 日志是出事后查，监控是事前盯住；队列堆死/模型挂了不靠用户投诉发现 | 第 11b、19 项后 | 1 会话 |
| 26 | H 可观测 | 压测演练（wrk/locust）：承压层全就位后模拟高并发 | "请求无限"预设的验收考试，压出真实瓶颈在哪一环 | 第 8~14、30 项后，靠近部署（异步改造属承压层，压测须等它就位才压得准） | 1 会话 |
| 27 | G 安全 | 角色权限（RBAC）：users 表加 role 字段（editor/senior_editor/admin）＋路由级权限校验＋前端按角色藏入口＋敏感操作审计（谁何时触发微调/入库，留不可抵赖记录）＋**数据可见范围策略（✅ 增 2026-09-06）** | 首个真岗位：微调入口、语料入库仅高级编辑可见可触发，普通编辑不可见；**与第 3 项分工重写——第 3 管认证＋默认行级数据归属（每张业务表挂 user_id），本项管角色＋功能级授权（能不能用某功能）＋数据可见范围（普通编辑只看自己的书、高级编辑/管理员看全部人的书，见规格 §四），不重复**；安全硬要求：后端必校（直接调接口也拒 403），前端藏入口只是体验；完成后给第 24 项与挂起的微调集成回补权限闸 | 第 3、4 项后 | 1 会话 |
| 28 | C 承压 | **SSE 推送通道（✅ 用途变更 2026-09-06）**：**只做后端 SSE 端点**（读第 11a 项 `tasks.progress_json` 推给前端）；**用途从「大纲逐字流式展示」改为「任务进度事件推送」**（推进度不推正文流）。P 的流式变体（DashScope stream 参数）与前端 EventSource 消费**均不在本项**——前者本轮不做（等真有逐字展示需求再加），后者归第 4g 项 | 规格明确「不展示 AI 思考过程、只显示请等待与正在写第 N 节共 M 节」，故逐字展示无岗位；SSE＝服务器单向推送（HTTP 原生），比 WebSocket 双向通道轻且对位 | 第 1、11a 项后 | 半会话（原半~1，前端部分移交 4g 后收窄） |
| 29 | F 加码 | WebSocket 统一实时消息通道：每登录用户一条常驻连接，承载任务进度/完成通知/审计事件广播 | 学习目的进场（本人明确要求），岗位＝大厂常见的"WebSocket 管消息推送＋SSE 管生成流"分工（**注：第 28 项 SSE 用途已于 2026-09-06 改为任务进度事件推送、不再管生成流，故本项岗位相应变为「进度与通知统一走 WebSocket，SSE 退役或与之并存」，做时当场裁定**）；严格说推送单向、SSE 可顶，故为加码项，熔断时与第 12、21 项同批优先砍；做时与第 28 项对照学（两者区别当场说清） | 第 28、11a 项后 | 半~1 会话 |
| 30 | C 承压 | 异步改造（async/await）：P、RAG 召回、数据库 IO、外部 API 调用等同步阻塞点改异步；流水线内可并行的智能体步骤并发化 | 治本“同步阻塞”——第 8 项多 worker 只是多开进程分担，单 worker 内一个慢请求仍占死；“请求无限”预设下 IO 不阻塞才真扛并发，与第 8 项配套 | 与第 8 项配套，建议在第 8 项前后做 | 2~3 会话 |
| 31 | B 欠账 | 图片素材接 Qwen-VL 转述链（模块 5 扩展方向）：教材图片素材上传后经统一推理入口 P 调 Qwen-VL 转成文字描述，再复用第 24 项入库链向量化进库、参与检索。开发期走外部 Qwen-VL 学转述链；生产期图片也属出版社数据、出域合规与第 23 项冲突，届时二次裁定（本地 VL/出版社特批/降级文字描述） | 教材含图片素材，图片不进知识库则检索缺图片内容；分工＝Qwen-VL 管图片转述、Qwen Embedding 管文字向量化（见认知架构图） | 第 6、24 项后（转述成文字后复用第 24 项入库链） | 1 会话 |
| 32 | B 欠账 | 负面清单 U、重写循环完善（模块 7 扩展方向）：教案生成后按负面清单 U 自检，不合格触发重写循环（经 P 反复调模型），直到合格或达重写上限 | 一次生成质量不稳，负面清单 U 是质量闸、重写循环是兜底；模块 7 流水线基础版已建（见 §7），此处补到生产可用，与“写作→审核→重写”环节衔接 | 第 1 项后（重写循环反复调 P，靠其超时/重试兜底） | 1 会话 |
| 33 | B 欠账 | 完整初稿生成流程打通（模块 7 最后一公里）：Pipeline 拆两步交互——①`POST /outline` 传 topic 返回大纲、②`POST /generate` 传选定节内容走 RAG 召回＋writer＋review＋rewrite 返回初稿；前端加两步交互（展示大纲→选节→展示初稿）；修复现有 Pipeline 无"选节"环节的设计缺口 | 模块 7 各智能体函数已建，但编排层缺"选节"、无 API 端点暴露，流水线端到端跑不通；此项打通后前端才能调到完整生成流程，第 4 项前端重写才有端点可接 | 第 1 项后（多步 LLM 调用靠超时/重试兜底）；前置于第 4 项（前端重写需此端点）、第 28 项（SSE 流式需此端点） | ~1 会话（**✅ 已完成 2026-09-04；本项产出有四处被 2026-09-06 功能设计取代，见 §7 销账补记**） |
| 34 | D 数据 | **edit_pairs 语料导出脚本（✅ 新增 2026-09-06，跨线焊点）**：把 `edit_pairs` 表里三类对照资料（用户问题→大纲、用户问题→正文小节、AI 正文小节→人工定稿）导出成主线② D3 微调训练格式 | 跨线焊点实体：主线① 攒原料、主线② 转成品。**约定＝表存原料、导出脚本转格式**，故主线② D3 训练格式未定不阻塞主线① 建表，两线不互相卡死；语料只收三类、只在提交那刻落库（规格 §五） | 第 2 项后（表就位即可写脚本）；主线② D3 格式定稿后补齐转换逻辑 | 半~1 会话 |
| 35 | B 欠账 | **全书一致性终审 Agent（✅ 新增 2026-09-06）**：整本生成完后跑一遍全书级检查——术语前后是否统一、有没有重复解释同一概念、前面留的伏笔后面有没有呼应 | 补四件套注入（规格 §7.1）覆盖不到的全书级联动：四件套管「写的时候别忘前面」，本项管「写完回头看全书」 | 第 36 项后（整本产物就位才审得了）；第 32 项后（审出问题走重写循环） | 1~1.5 会话 |
| 36 | B 欠账 | **规格 §九/§十 后端实现（✅ 新增 2026-09-06，33 项销账接棒；✅ 拆 36a~36f 2026-09-06——后端版 item4，不许压成 3~4 会话赶工降格）**：§九 新端点组＋§十 pipeline 函数＋rag/differ.py＋submit 事务＋意图判定＋整本串行编排，明细见 36a~36f | 33 项销账后端点/编排/四件套/diff/意图判定三不管的接棒者；不做则第 4 项前端有界面没接口、端到端跑不通 | 第 2、11a 项后；**前置于第 4、35 项** | 见 36a~36f，**合计 6~9 会话** |
| 36a | B 欠账 | §九 端点骨架＋books/sections 的 CRUD 与暂存版本链端点（POST /books、GET /books、GET /books/{id}、PUT outline/staging、GET outline/versions、PUT sections/{id}/staging、GET sections/{id}/versions） | 建书/列表/详情/暂存/版本链，偏 CRUD，是 36b~36f 的端点地基 | 第 2、11a 项后 | 1~1.5 会话 |
| 36b | B 欠账 | §十 大纲侧 pipeline 函数（outline_agent 改签名收 title＋requirement、parse_outline 解析节树、align_nodes 标题序列对齐保 node_key、revise_outline_agent 按指令改大纲、summarize_agent 摘要卡、build_glossary 累积术语表）＋端点 outline/generate、outline/revise | 四件套注入（规格 §7.1）的产出端；node_key 对齐是大纲变更后节一一对应的命门（规格 §八） | 36a 后 | 1.5~2 会话 |
| 36c | B 欠账 | rag/differ.py（diff 唯一实现）＋GET /diff 端点：页面三色标记与语料 diff_ops_json 共用同一份计算结果（规格 §7.3 规则 3，禁前后端各写一套） | diff 唯一真源；算错则页面显示与语料对不上、微调数据全废 | 第 2 项后 | 0.5~1 会话 |
| 36d | B 欠账 | POST /books/{id}/submit 整本提交事务：一个事务里清暂存＋建版本（outline_versions/draft_versions）＋落语料 edit_pairs＋改书 status＋版本链只留最近 5 版裁剪；drafting/submitted 分流作废语义（规格 §八） | 整本提交＝多表事务，裸 SQL 撑不住（呼应第 2 项 ORM）；语料只在此刻落库（规格 §五） | 36a、36c 后 | 1~1.5 会话 |
| 36e | B 欠账 | POST /intent 意图判定＋classify_intent：指令连同书状态（最新大纲/各节正文/空节）交 LLM 判意图，判成「改大纲会作废正文」先弹确认；**书状态上下文体量控制（承接第 5 项销账岗位）**——大书各节正文不能全塞须压缩（规格 §2.4） | 输入框指令的总路由；不可逆操作判错要加闸（规格 §2.4） | 36b、11a 后 | 0.5~1 会话 |
| 36f | B 欠账 | POST /books/{id}/draft/generate 整本串行编排 generate_book（18 节×审校串行＋四件套注入＋进度上报写 tasks.progress_json）＋POST sections/{id}/generate 补单节＋POST sections/{id}/rewrite 局部重写 rewrite_passage_agent（注入「用户已改句子」标记＋尊重约束） | 规格 §7.2 整本对用户一次、对模型一节一节串行；进度上报对接 11a 任务表、28 推送、4g 消费 | 36b、36e、11a 项后（审校/重写循环复用模块 7 现有 review→rewrite；第 32 项负面清单 U 是后续质量增强、非本项硬前置） | 1.5~2 会话 |
| 挂起 | — | P 接主线②适配器 | 卡主线②微调产出，不占位、就绪后插回；**基座型号须与主线②微调基座一致（见第 23 项），否则适配器挂不上** | — | — |
| 挂起 | — | 微调任务集成：前端提交入口→任务队列（复用第 11a/11b 项机制）→训练脚本（主线②产出）→适配器产物→P 加载 | 卡主线②阶段五/六，与适配器插回同批；产品定位＝系统性改进（攒对照数据批训）非逐篇急救 | 主线②就绪＋第 11a/11b 项完成 | — |
**P1 待办（✅ 登记 2026-09-06，功能设计明确本轮不做；登记备查、不占排期、不计入工期）**：

| 编号 | 待办 | 什么时候该做 |
|---|---|---|
| P1-1 | 节级前置依赖标注（显式标「第 5 节依赖第 2 节」，替代现在靠摘要卡覆盖联动） | 单本节数超过 50 |
| P1-2 | 按章并行、章内串行（现在是纯串行，见规格 §7.2） | 单本生成时间成为主要痛点 |
| P1-3 | 长任务取消（现在点错了只能等它跑完） | 用户开始抱怨停不下来 |
| P1-4 | 公式渲染与字符 offset 共存（MathJax 重排 DOM 会打乱 offset，与局部重写、三色 diff 直接冲突；现延续模块 7「初稿禁公式」，与第 7 项公式渲染同源） | 教材必须含公式时 |

执行原则：全部走 B 档（每行说得清才合入）；影响正确性的问题当场修，性能/架构类按台账排期；每项完成后遗留区销账一行；基础设施（MySQL/Redis/APISIX/nginx/Prometheus）一律 Docker 拉起，不装 Windows 原生版；依赖清单（requirements/pyproject）第 2 项开工前补齐（第 0 项建仓时漏锁，2026-09-06 查证项目无此二文件）。
时间预算（**✅ 重算 2026-09-06 产品功能设计定稿后；✅ 二次重算 2026-09-06 台账审查后；✅ 三次重算 2026-09-06 item36 拆分后**）：第 0 步约 **50~63 次会话**（≈ 17~21 周，含第 30 项异步改造 2~3 会话、第 31/32 项各 ~1 会话）。较原 34~39 **净增 16~24 会话**，逐项对账：第 4 项拆 4a~4g 净增 **8~10**（原 1~1.5 → 9.5~11.5）＋第 2 项增 **1**（表从 3 张涨到 10 张，1~1.5 → 2~2.5）＋第 11 项拆 11a/11b 净增 **0~1**（原 2 → 11a 1~1.5 ＋ 11b 1~1.5，11a 提前为硬前置）＋第 34 项 **0.5~1**＋第 35 项 **1~1.5**＋**第 36 项 6~9（拆 36a~36f，承接 §九/§十）**＋第 4a 扩登录页/新建表单 **0~0.5**−第 5 项销账 **0.5**；第 28 项因前端部分移交 4g 而收窄（半~1 → 半），抵销不计入净增；P1 待办不占排期不计入。之后部署/文档/复盘/毕业另留 ≥ 2 周，阶段七全程约 **19~23 周**。
熔断条款：外部硬节点＝主线②开学考核（时间未定，以深度学习方案登记为准）；时间一定，备考期本线暂停或压缩会话，阶段七工期顺延；若顺延后算不平，再按序砍队尾（第 21 项）→加码项（第 12、10、29 项）→**第 35 项全书一致性终审与 P1 待办同批可砍（✅ 补 2026-09-06：终审属质量加分项，砍了不影响端到端跑通；P1 本就不占排期）**，B 组欠账与 C/D/G 承压安全层不动。**第 4a~4g、第 11a、第 34、36（含 36a~36f）项不在可砍范围**：第 4a~4g、11a、36a~36f 是功能规格跑起来的最小集，第 34 项是跨线焊点（砍了主线② D3 第三类语料断供）。
排除裁定（✅ 定 2026-09-01）：配置中心/服务注册发现/灰度发布等微服务组件不进表——本项目是单体，这些工具治"服务多到管不过来"的病，单体无岗位；拆微服务之日再登记。服务器成本不属本项目预算：生产硬件由出版社采购（见 §3），学习阶段全部组件开源免费、Docker 本机拉起，笔记本即数据中心。


**AI 使用三档制**：

| 档位 | AI 可以做什么 | 前提条件 |
|---|---|---|
| A 档（教练） | 埋雷、翻译报错、当橡皮鸭 | 默认档，任何时候可用 |
| B 档（扩展） | 把手写通过的最小版扩展成生产版 | 该模块最小版已手写跑通并通过口述验收 |
| C 档（生成） | 直接生成样板代码 | 仅限已能说出该类代码结构的模块，生成后逐行验收 |

红线：**任何档位下，AI 产出的代码没经过你"为什么这么写"的逐行解释，不许合入项目。**

**协作代码验收（✅ 定，2026-08-21）**——项目与他人协作时，别人写的代码按以下规则处理：
1. 别人拿进项目的代码 = C 档生成代码同等待遇：你说不清"为什么这么写"就不许合入（同一红线，对人同样适用）。
2. 分级要求：主线核心模块（阶段六表内 7 个）每行都要说得清——毕业抽问范围；外围模块（协作者的运维脚本、边角胶水）至少说清顶层——干什么的、和哪个接口相接（同"AI 应付项的底线"标准）。
3. 分工先保练习：我正在练的模块必须我写；协作者先写了的，二选一——换练习模块，或对其代码做逐行验收把它"变成"我的。


---


## 2. 时间管理

**开学版（✅ 定，2026-08-31）**：按课表（d:\Learning\课表.md，唯一事实源）核定。学习日＝周一~周五＋周日（周六休息零额度；周日晚百年党史为水课，零投入不计），一天一条线与深度学习线轮转，每次会话 1h 保底、2h 理想 → 本线每周约 3 次会话（3~6h）。节假日（中秋落在第 5 周、国庆落在第 6~7 周）按届时实际放假安排：假期不强制学习，轮转顺延不重置。若算不平→深度学习线降级为只应付社团作业。

会话组成（时长按实际会话时长等比压缩）：

| 环节 | 内容 |
|---|---|
| 开场 | 默写复盘：说不出来的就是今天第一任务 |
| 主体 | 手写 + 埋雷修雷；资料只查马上要用的那一小块 |
| 收尾 | 口述今天学了什么，教练对照当天收件箱条目检查 |

原则：
- 时间花在"卡住-想通"上，不是行数上。10 行真懂 > 200 行抄写。
- 卡住 30 分钟必须停手，写下"期望/实际/猜想"三行再求助。
- **周末加餐＝bug 排查训练**（✅ 定，2026-09-03）：每周固定 1~2 次、每次 ≤1h，挂本时段，不额外占两条主线时间额度（时间账仍按本节「每周约 3 次会话」算平）。规则由 bug 排查师守则（`d:\Learning\.qoder\rules\bug_guidelines.md`）管：在真实项目里注入生产级 bug，本人自己找证据定位，AI 只出题＋监考＋复盘，不给答案。开工前置＝台账第 19 项第一批已落地 ＋ git 工作区干净。原「本周修过的 Bug 换个形式再埋一次」保留为其中一种题型。


---


## 3. 环境与硬件约束

- **后端启动命令（焊死，2026-08-21 随项目目录迁移更新）**：永远从项目根（d:\Learning\pub_of_agent）执行
  `d:/Learning/pub_of_agent/.venv/Scripts/python.exe -m uvicorn rag.app:app`
  禁止 `python rag/app.py` 直接跑（会撞 `'rag' is not a package`）。原理见收件箱《同名模块的身份随启动位置变化》《uvicorn 启动命令的模块路径按执行位置解析》。
- **部署目标：本地化部署＝部署在出版社提供的目标环境（数据不出域），不是跑在自己笔记本上**。学习阶段跑本机小规格 + API；生产硬件（见蓝图 §6 分档）由出版社采购。
- 硬件上限：显存 8G → Ollama 只跑 ≤7B 的 Q4 量化模型（约 5G）；embedding 用轻量版（≤1G）。超规格不考虑。
- 上半场不碰 GPU（mock LLM + TF-IDF 纯 CPU）；Ollama 到下半场接真实 LLM 时才装。
- 教材智能体原型阶段：LLM 走 DashScope API（账号已有/可申请，见蓝图 §9 原型档），不占本地显存；本地模型到蓝图第 2 阶段再引入。
- pip 环境（2026-08-26 实测）：包源＝pypi.org 默认，未配置任何镜像（pip config 无 index-url）；全局缓存目录＝D:\pip-cache（装过的包下次免下载）。已知坑：开着代理软件（虚拟网卡/隧道模式）访问 pypi 会被规则分流拦断，报 SSL UNEXPECTED_EOF_WHILE_READING——先关代理重试；仍不通再用阿里云镜像兜底：`pip install 包名 -i https://mirrors.aliyun.com/pypi/simple/`。
- 微调（fine-tuning）归主线2承担（见深度学习方案阶段五/六），主线1不重复学。


---




---


## 5. 外部项目与学习主线的映射

**总纲：两条学习主线——①全栈+智能体（本方案）；②深度学习（飞桨）。主线上每一行都要说得清；其余事项一律 AI 代写应付。**

| 事项 | 定位 | 投入原则 |
|---|---|---|
| 教材智能体学习项目（本目录 `pub_of_agent`） | 主线①毕业作品 | 按阶段七台账继续推进，慢做细做；知识库搭建＝RAG 升级素材；本地部署＝§3 的 Ollama 部分；适配器微调＝主线②内容，学一次两处用。项目方案见 §1 下半场（仅设计参考，不照搬） |
| 出版社原型工作任务 | 协作型外部项目（✅ 拆分登记，2026-08-26：外部已有全栈原型 publisher-agent-release 部署上云供出版社试用） | 工作代码一律进独立目录，不进 `d:\Learning`；AI 生成须过三档制 B/C 档验收（逐行说得清或至少顶层说得清），绝不似懂非懂交付；协作代码验收规则同等适用。外部原型缺口（无数据库/无鉴权/BM25 检索/无 LoRA）恰为主线①②在练内容，适配器产物可经 P 插入其原型交付 |
| 飞桨/深度学习 | 主线② | 独立项目、独立守则+方案，不与主线①混账 |
| 绘画智能体 | 封存 | 需求未定，零投入 |
| 大创 AGENT | 封存 | 无人催，零投入 |
| 万方 | 最低投入维护 | AI 代写应付，不占学习时长、不算学习成果 |
| 课程/实验报告 | 固定成本 | 固定时段压缩处理，不许侵蚀学习时段 |
| 社团活动 | 事件驱动 | 来了应对，不预留整块时间 |

**风险规则（任务来得比学得早时）**，只有两条路：
1. 先学后做：能纳入主线的就当练习素材；
2. 走三档制 B/C 档：AI 写＋你逐行验收。

绝不允许"似懂非懂接手"。AI 应付项的底线：交出去的项目至少能说出顶层是干什么的。


---


## 6. 踩坑清单（一行索引，格式规则没有我要求不许修改）

> 用途：知识全文进收件箱→归档飞书后，教练在场内就够不着了；本节只存大坑的"一行永久副本"，每次新会话开场和遗留问题区一起查。
> 格式：【日期】症状 ｜ 根因一句话（全文：飞书文档标题）。

- 【2026-08-18】uvicorn 从 rag/ 目录启动 → `'rag' is not a package` ｜根因：模块身份随启动位置变（全文：飞书《同名模块的身份随启动位置变化》《uvicorn 启动命令的模块路径按执行位置解析》；已焊进 §3 启动命令）

进出规则：
- 什么才进：① 卡了 30 分钟以上；② 概念反直觉、容易再踩。普通笔误不进。
- 什么时候进：周末归档收件箱时，教练对每条问一句"要不要升进踩坑清单"，本人拍板。
- 什么时候出：同一坑换形式重埋过、本人修对了，划掉。


---


## 7. 遗留问题（教练登记区）

> 记录"验收没答上的追问"和"阶段遗留事项"，每次新会话开场先查这里。
> 只记任务和问题，不记知识点（知识点进 d:\Learning\feishu_manage\收件箱.md）。
> 简化规则（✅ 定，2026-08-27）：完成的模块只留一行结论（完成时间 + 仍生效的决定），验收过程、收件箱条目清单、过程复盘一律不登记；只记未销账的任务、待确认事项、跨模块决定。


已完成（一行结论，过程细节不登记）：
- ✅ 阶段二/三/四验收全部通过（2026-08-18 ~ 08-21）。
- ✅ 阶段五认知架构图二轮验收通过（2026-08-21）。
- ✅ 模块 1 数据库（2026-08-24）。
- ✅ 模块 2 鉴权（2026-08-24）。
- ✅ 模块 3 前端框架（2026-08-25）。仍生效的决定：范围收缩——只做 Vue CDN 最小重写，组件库与生产版重写留阶段七扩展补齐。
- ✅ 模块 4 多轮上下文（2026-08-26）。仍生效的决定：滚雪球膨胀按最小版认账不修，靠 fetch_history(5) 条数上限兜底；截断/摘要留扩展方向。
- ✅ 模块 5 embedding 升级 + 向量知识库（2026-08-28）。仍生效的决定：embedding 走 DashScope text-embedding-v4；向量库＝项目内 JSON 文件（块文本+向量一起存），不上向量数据库，扩展再换；key 走项目内 .env；逐块调 API，批量调用留扩展方向；旧 TF-IDF/旧 dict 版 cos 已删（用户自删，不留回退）。
- ✅ 模块 6 统一推理入口 P（2026-08-28）。仍生效的决定：所有生成文本只走单一入口 LLM()，走 DashScope OpenAI 兼容 Chat Completions（qwen-plus）；多轮历史以 user/assistant 消息对进 messages，字符串拼接版 build_history_prompt 废弃；取历史留在 app.py，P 内部不读库（保住 P 通用性）；超时/重试、换官方 SDK、接主线②适配器留阶段七扩展。
- ✅ 模块 7 智能体流水线（2026-09-01，阶段六收尾）。仍生效的决定：基座模型 qwen-plus 经 P 走通，适配器就绪后切换（跨线依赖主线②）；查重用手写 n-gram 数重叠（set 去重 + in 命中，重复率保 [0,1]），比对底本＝全库原始语料；职责边界焊死——召回与存库归编排层，写作智能体不做检索；重复退化三件套已修（写作 prompt 篇幅上下限 + 禁复读、temperature 0.5 + repetition_penalty 1.2）；仍欠的账：初稿禁公式是最小版绕法，公式渲染（MathJax 类）留阶段七前端展示时还；语料单薄导致初稿空泛，靠语料铺开解决（主线②素材准备）。

- 【局势登记】（2026-08-26）外部出版社侧原型已部署上云。仍生效的裁定：学习与工作目录分开（见 §5 拆分登记）；毕业作品定位不变；工作任务走协作型外部项目条款。待确认：① 本人是否参与该原型的后续开发维护 ② 出版社素材流向——课标/参考书/范文预计可正常获取（撑住主线② D3 语料第 1、2 类）；第 3 类"AI 初稿→编辑定稿"对照数据能否获取取决于待确认①，拿不到用本人学习项目自产对替代，不阻塞。**（✅ 2026-09-06 更新：替代路径已落实为台账第 34 项——主线① `edit_pairs` 表存三类对照原料，导出脚本转主线② D3 格式，不依赖外部原型侧供数据；同时主线② D3 条目已补第二个焊点。）**

- 【API key 配置事件】裁定已进教练守则（扩展须与实际开发对齐；敏感配置走项目内 .env）。
- ✅ 阶段七第 0 项 git 建仓（2026-09-03）。仍生效的决定：.gitignore 挡密钥/环境/缓存/备份/生成物（.env/.venv/__pycache__/backup/vector_store.json）；源码/文档/语料源/规则配置（.qoder）进版本库受保护；commit message 惯例要写具体说明这一版是什么。三问复述已补；远程仓库已建（https://github.com/Zack673-cpu/pub_of_agent.git，main 分支）。
- ✅ 阶段七第 1 项 P 加超时/重试（2026-09-03）。仍生效的决定：P 函数（llm.py）和 get_embedding（rag.py）都加了 timeout=30、retry 3 次、backoff（2^attempt 秒）、raise_for_status、错误处理（区分 Timeout/ConnectionError/HTTPError/KeyError/ValueError）。
- ✅ 阶段七第 33 项 完整初稿生成流程打通（2026-09-04）。仍生效的决定：Pipeline 拆两步交互——`generate_outline(topic)` 生成大纲、`generate_section(section)` 内部读向量库＋召回＋写作＋审校改写循环；app.py 加 `POST /outline` 和 `POST /generate` 两个端点；前端加两步交互 UI（主题→大纲→选节→初稿）；原有 `/ask` 聊天功能不动。
  - **【销账补记】（2026-09-06 产品功能设计定稿后，本项产出有四处被取代）**：① **“选节”交互降级**——新版对用户是整本一次生成、无选节环节，选节能力保留为“补生成单节”（`POST /sections/{id}/generate`，用于大纲新增的空节点）；② **`POST /outline`、`POST /generate` 两端点删除**，被规格 §九 的新端点组取代；③ **`/ask` 自由问答下线**（原销账写“不动”，现改）——输入框只用来下指令、界面不显示对话，自由问答没地方待了；④ `generate_outline` 签名要改（收 title＋requirement，不再只收 topic），`generate_section` 内部逻辑不动但外层由 `generate_book` 串行编排接管。
- ✅ 阶段七第 19 项第一批 最小证据链（2026-09-04）。仍生效的决定：app.py 三个端点 except 加 logger.error + exc_info=True（终端＋文件双写）、启动时打印 API key / 向量库 / 语料存在性检查、错误 detail 透传 str(e)、rag.py 路径锚 `Path(__file__).parent`、日志文件 app.log 也锚 `__file__`。

- ✅ 产品功能设计定稿（2026-09-06，总监督主持，4 轮修正）。仍生效的决定：**交付规格＝`pub_plan/产品功能设计定稿.md`**（前端后端照它实现、照它逐条验收；与蓝图分工不合并）。核心裁定：① 两页面（写作/历史）＋可收缩左侧栏＋「+ 新建」；② **界面不留任何对话记录**，指令发出即消失、不留痕；③ 大纲块钉在 AI 显示区底部、只显示最新一份、可编辑（结构化树）、有自己的版本链，右侧栏只放正文不放大纲树；④ 固定提示行由程序写死、**只在检测到大纲有改动时出现**；⑤ 输入框指令**交 LLM 判意图**（索引块只作定位锚点、不作判定依据），判成「改大纲且会作废正文」时先弹确认；⑥ 局部重写**不给硬边界**，AI 自主判断范围，提示词约束「不许改用户手改过的内容、必须改时保住原意」；⑦ 版本走 **git 式直线**——只有提交产生版本、只能在最新版改、只保留最近 5 个已提交版本、改旧版需「以此版为准继续修改」并弹确认舍弃后续；⑧ **大纲与正文享受同一套**版本/三色标记/看改两态/切版自动对焦；⑨ **整本一次提交**，提交按钮只在右侧栏；⑩ 语料只收三类（用户问题→大纲、用户问题→正文小节、AI 正文→人工定稿）、只在提交那刻落库，**局部重写要进语料**（先前判定已修正）；⑪ 权限按角色分可见范围（普通编辑看自己、高级编辑/管理员看全部）；⑫ 生成粒度＝对用户整本一次、对模型一节一节串行；⑬ 前后联动靠四件套注入（全书大纲＋滚动摘要卡＋累积术语表＋前一节尾段）。台账影响：第 2 项升硬前置且粗估翻倍、第 3 项数据隔离表述改、第 4 项拆 4a~4g 并裁定 Vite、第 11 项拆 11a/11b 且 11a 提前为硬前置、第 27 项增数据可见范围职责、第 28 项 SSE 用途改任务进度、第 33 项销账补记四处变更、新增第 34/35 项与 P1 待办四条、**增第 36 项承接 §九/§十 后端实现（33 项销账接棒）、第 5 项销账（/ask 下线岗位消失）、第 7 项公式渲染移出（并入 P1-4）**、工期 34~39 → **48~58 会话**（净增 14~19，逐项对账见台账时间预算行）。**本轮同时理清了一处职责重叠：11a（产生进度数据）/ 28（后端推送通道）/ 4g（前端消费）三项分工焊死，不成环。**

- ✅ 台账第三轮深审（2026-09-06，总监督主持）。仍生效的决定：① **跨线基座型号须两线钉同一个**（item23＋主线②D3/阶段六）——三选一只管格式不管型号，开发期 qwen-plus（云、闭源、不可本地微调）≠ 生产期本地基座，部署前须用生产基座回归测 prompt/行为；② **item36 拆 36a~36f、重估 6~9 会话**（后端版 item4，原 3~4 是降格，不许赶工）；③ **切本地 embedding 须全量重建向量索引**（item23＋item15 衔接，旧向量与新模型空间不兼容）；④ 规格 §八 **staging 去掉「NULL=大纲暂存」残留**、大纲暂存只归 outline_staging、staging 只管正文（section_id 必填）；⑤ **conversations 表岗位待定**——/ask 下线＋指令不留痕后用途不明、与 tasks 疑似重叠，item2 建表前裁「定岗或 drop」；⑥ **item4 路由去掉微调页**（归挂起项，4a 只建写作/历史两入口）；⑦ **item4 与 36 依赖细化**——4a 纯脚手架可与 36 并行，4b/4d/4g 及新建表单 submit 须等 36 端点。工期 48~58 → **50~63 会话**（item36 +3~5），阶段七全程 19~23 周。

- ✅ 数据模型定稿复核（2026-09-07）。7 条裁定：① conversations 表 drop（/ask 下线＋不留对话不留痕，无岗位）；② staging.book_id 删掉（通过 section_id→sections.book_id 可查，不冗余）；③ tasks.kind 六种够用不补；④ 版本链 5 版裁剪放在 submit 事务里，靠 per-book version_no 排序；⑤ outline_staging（大纲暂存）与 staging（正文暂存）读写路径确认无断点；⑥ edit_pairs.granularity 三种对规格 §五 三类语料一一对应；⑦ sections.discarded 靠 books.status 分流作废语义（drafting 物理删、submitted 标记删）。

- 【下次开工】（2026-09-07 更新，接续第 2 项）数据模型复核已过（7 条裁定已登记）；ORM 模型只写了 User + Book 两张（导入已修对），**剩 8 张表（outline_versions / outline_staging / sections / draft_versions / staging / edit_pairs / rewrite_requests / tasks）下次先补齐**，然后再进 Alembic 初始化 + 迁移基线 + drop 旧表。

