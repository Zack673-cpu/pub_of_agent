# 学习方案（主线①：全栈 + 智能体）

> 本文件管"学什么、什么顺序、怎么算完成"；怎么学见 `.qoder/rules/coding-guidelines.md`（教练守则）。


---


## 0. 总目标

**独立产出完整项目**——功能完整、结构规范、可部署、可持续迭代。

核心标准：**每一行代码（无论谁写的）都能说出为什么存在。**
手写是刻意练习手段，不是终点；练成之后你负责架构决策和验收，AI 可以帮你堆量。

毕业考试（三问全过才算毕业）：
1. 有一个拿得出手的完整项目（可部署、有真实功能、有文档）。
2. 任意模块随机抽问"这段为什么这么写"，答得上来。
3. 能独立排查生产级 Bug（AI 只当橡皮鸭，不给答案）。训练机制＝每周固定 1~2 次 bug 排查训练（时段见 §2 周末加餐，规则见 bug 排查师守则）；硬前置＝台账第 19 项第一批「最小证据链」必须先落地，否则项目本身吐不出证据，练的是猜不是查。


---


## 1. 整体路线

```
上半场（刻意练习，全手写，AI 只当教练）
   零：热身（3 天封顶）
   一~四：压舱石三件套 —— 手写 RAG → 裸 API → 丑前端 → 删掉默写重写

下半场（真实项目，你主导 + AI 按三档制辅助）
   五：选定完整项目，画架构图
   六：逐模块推进（数据库→鉴权→前端框架→多轮→embedding→推理入口→智能体流水线，见阶段六）
   七：扩展补齐 + 部署 + 文档 + 复盘，毕业
```


### 阶段零~四：热身 + 上半场压舱石训练营（✅ 已完成）

看课（B站 RAG 入门，带过滤器）+ 知识闸门（Python 基础 / RAG 管道六步）→ 30 行手写 RAG（`rag/rag.py`）→ FastAPI 裸接口（`POST /ask`）→ 极简前端（`index.html`）→ 删掉重写默写版。关键取舍：先 RAG 后 Agent、全栈从后端开始、禁区（厚框架 / 多智能体 / 向量库调优）阶段一~四不碰。


---


### 下半场：完整项目

#### 阶段五：选定项目 + 架构图（✅ 已完成，2026-08-21）

- **完整项目＝教材智能体（出版社智能体）**（✅ 定 2026-08-19）。
- **语言＝Python**（与上半场成果、主线②飞桨同生态；本地小规模部署不需换 Go）。
- 项目方案 `pub_plan/教材智能体方案设计.md` **仅设计参考、不是交付规格**，按学习路线裁剪分阶段做，不为对齐蓝图而“似懂非懂接手”；方案不含微调，微调由主线②承担。
- 裁剪版认知架构图 `pub_plan/认知架构图.md` 二轮验收通过，为总验收标准（已学讲内部细节、未学答盒子三问）。


#### 阶段六：逐模块推进（✅ 已完成，2026-09-01 收尾）

7 模块（数据库 → 鉴权 → 前端框架 → 多轮上下文 → embedding+向量知识库 → 统一推理入口 P → 智能体流水线）已全部走“最小手写 → 口述 → AI 扩展（B 档）→ 逐行验收”完成（各模块仍生效决定见 §7）。各模块扩展方向已全部迁入阶段七台账：ORM→第 2、登录注册→第 3、框架重写→第 4、截断摘要→第 5、图片 Qwen-VL 转述→第 31、超时重试→第 1、负面清单 U/重写循环→第 32、批量 embedding→第 6、接主线②适配器→挂起项。

SSE 裁定（2026-09-02 激活）：大纲生成采用 SSE 流式显示（落地见台账第 28 项）；长任务状态轮询机制不变。

**阶段六推进规则第 4 条（企业级标准，仍生效）**：阶段七第 0 步扩展补齐时以大厂企业级标准审视整个项目——具备应对高并发的能力，凡与正式生产标准不符的提出修改，确保部署后（除硬件环境限制外）可直接当产品上市方向靠拢、方方面面尽可能好。


#### 阶段七：毕业

| 顺序 | 模块 | 产出 | 验收标准 |
|---|---|---|---|
| 0 | 扩展补齐 | 各模块生产版（见阶段六推进规则第 4 条） | 每个补齐部分走逐行验收 |
| 1 | 部署 | 部署脚本/流程 | 项目能在目标环境跑起来（本地模拟出版社环境；nginx 等新技术届时以新模块身份登记进场） |
| 2 | 文档 | README | 陌生人能照着跑起来 |
| 3 | 复盘 | 每篇 200 字复盘 | — |
| 4 | 毕业考试 | — | 三问全过：有完整项目（可部署、有真实功能、有文档）；任意抽问"这行为什么这么写"答得上来；能独立排查生产级 Bug |

**第 0 步扩展补齐台账 v2（生产硬化版，✅ 定 2026-09-01）**：预设背景＝请求无限＋语料无限，项目在此前提下要尽可能扰得住。范围锁死在此表，表外新需求一律先登记再裁定，不许边补边长。每个工具必须在本项目里有真岗位，不做孤立 demo（新工具进场按教练守则"新阶段开场"走新词盘点）。编号＝登记 ID 非执行序，做哪项看"依赖"列。

| 编号 | 组 | 项目 | 在本项目的岗位 | 依赖 | 粗估 |
|---|---|---|---|---|---|
| 0 | A 基础 | git 建仓：本地 git init + 连远程仓库（GitHub）+ 首次推送 + 上游追踪（.gitignore 已有，按需补充） | 版本安全网＝必须有异地远程备份；只在本地一块硬盘上的 git 不算安全网，先于一切扩展 | 无 | 半会话内 |
| 1 | B 欠账 | P 加超时/重试 | 网络抗抖，全项目受益 | 无 | 半会话 |
| 2 | B 欠账 | 裸 SQL 换 ORM（SQLAlchemy）＋Alembic 迁移；须为现有三张表建 Alembic 迁移基线（alembic stamp 标基线或初始迁移含现有表），否则迁移历史接不上已存在的库 | 改表结构不再手改库；做了迁移就得把现有库纳进迁移体系才算达标 | 无 | 1~1.5 会话 |
| 3 | B 欠账 | 完整登录注册（多用户） | 无 token 调接口被拒；安全硬要求：密码 bcrypt 哈希禁明文，token 过期/刷新/吊销；多用户硬要求：users 表＋业务表加 user_id 归属＋数据隔离（各编辑的对话/稿件互不可见，取历史只取本人） | 第 2 项先做 | 1.5 会话 |
| 4 | B 欠账 | 前端组件库重写 | 多组件复杂状态，框架回本；组件库定 Element Plus（Vue3 生态主流，靠组件拼装＋布局模板出观感，不手写 CSS）；多用户硬需求：多页路由（vue-router：登录/写作/稿件/微调页）＋全局状态（Pinia：登录态/角色跨组件共享）；构建方式（Vite 工程化 vs 继续 CDN）开工时裁定 | 无 | 1~1.5 会话 |
| 5 | B 欠账 | 多轮历史截断/摘要 | 滚雪球膨胀根治 | 无 | 半会话 |
| 6 | B 欠账 | embedding 批量调用 | 建库提速 | 无 | 半会话 |
| 7 | B 欠账 | 公式渲染（MathJax）＋查重性能优化（现全库逐篇算 n-gram 重叠，百万语料必慢；优化＝建 n-gram→文档 倒排索引或 MinHash 候选召回，只比对命中候选不逐篇全库扫。达标：查重延迟随语料规模亚线性增长，百万语料下单篇查重秒级返回） | 公式＝教学产品刚需；查重性能＝“语料无限”预设下手写全库比对撑不住，须索引化 | 靠近部署时做，与第 15 项索引思路对照 | 零散~1 会话 |
| 8 | C 承压 | 多进程服务（uvicorn 多 worker） | 单进程扰不住并发，多开 worker 分担；但每个 worker 内同步阻塞仍在，治本靠第 30 项异步改造，两者配套（多 worker ＋ worker 内异步） | 无 | 1 会话 |
| 9 | C 承压 | nginx 反向代理＋静态资源＋TLS 加密 | 统一入口，全站禁明文传输（内网用自签证书） | 第 8 项后 | 1 会话 |
| 10 | C 加码 | APISIX 网关（路由/负载均衡/限流） | 网关分发；限流＝网关级粗限流（入口/IP/接口 QPS 总量），与第 13 项 Redis 应用级细限流分层不重复；与 nginx 职责重叠处开工时裁定合并或共存 | 第 9 项后 | 1~2 会话 |
| 11 | C 承压 | Redis 任务队列＋后台 worker＋前端轮询；任务状态落库（持久化）、失败重试/标败策略 | 流水线长任务不占接口，焊"长任务用轮询"老决定；worker 崩了任务不丢、失败有交代。并发三解法分工：本项队列＝长任务移出请求周期（架构级）、第 8 多 worker＝多进程分担（进程级）、第 30 异步＝单 worker 内 IO 不阻塞（协程级），三者互补不替代；量大后队列职能由第 12 项 Kafka 接管 | 无 | 2 会话 |
| 12 | C 加码 | Kafka 消息队列 | 无限任务预设下接管重任务流：Kafka 上线后第 11 项 Redis 队列职能收敛，Redis 退守缓存（第 13 项），不搞两套队列并存 | 第 11 项后 | 1~2 会话 |
| 13 | C 承压 | Redis 缓存＋限流 | 热点召回缓存；限流＝应用级细限流（用户/业务配额，如每编辑每分钟生成次数），与第 10 项网关级粗限流分层，挡恶意刷接口 | 与第 11 项共用 | 1 会话 |
| 14 | C 承压 | Docker Compose 容器化，含生产级编排细节（healthcheck 健康检查、restart 崩溃自恢复、mem_limit 内存限）；healthcheck 探针调第 19 项应用 /health 端点，不各做各的 | MySQL/Redis/APISIX/nginx 一把拉起，可部署地基 | 各组件就位后 | 1 会话 |
| 15 | D 数据 | 向量库升级（JSON → FAISS 类索引），含索引持久化（write_index 存盘 / 启动 read_index 加载，不随进程重启重建）＋增量写入（供第 24 项语料入库调用）；分库策略＝每教材一独立索引，检索按当前教材路由到对应索引（教材间语料隔离、按教材增量重建、检索空间小），跨教材检索需求出现再加聚合层 | 百万语料下逐条算 cos 必死；升级成“库”就必须能存盘复用，否则每次重启重建百万向量不可接受 | 无 | 1 会话 |
| 16 | E 规范 | pytest 自动化测试（查重/检索/鉴权等核心逻辑与接口） | 毕业考试排查 Bug 的地基 | 无 | 1.5 会话 |
| 17 | E 规范 | CI 流水线（GitHub Actions：推代码自动跑 pytest 测试 + ruff 静态检查，两者都过才放行） | 坏代码进不了仓库＝测试不过或风格/静态检查不过都拦下；只跑测试则烂风格代码照样进仓，不达岗位标准 | 第 0、16、18 项后（ruff 见第 18 项） | 半会话 |
| 18 | E 规范 | 代码规范（ruff）＋OpenAPI 文档 | 大厂基本盘 | 无 | 半会话 |
| 19 | E 规范 | 结构化日志＋错误处理＋健康检查＋链路追踪（request_id 贯穿流水线），日志脱敏（用户提问/token 不落盘）。**分两批（✅ 拆 2026-09-03）**：第一批＝最小证据链——app.py 的 `except Exception` 除转 HTTPException 外必须同时落日志（终端＋文件），异常带上下文（哪一步、什么输入），错误 detail 可读不含糊，服务启动时打印关键配置加载结果（.env 是否读到、key 是否为空、向量库/语料路径是否存在），`rag.py` 第 14/16 行相对路径改 `Path(__file__).parent` 锚定；第二批＝健康检查＋request_id 链路追踪＋日志脱敏 | 上线出事能查，且不留泄密后门；多智能体长链路一次请求能追到底。**第一批同时是 bug 排查训练的前置**：现状 app.py 把异常转成 HTTPException(500) 后 uvicorn 视为正常业务响应、不打堆栈，后端终端只有一行访问日志，查不到根因；且相对路径导致换目录启动就 FileNotFoundError，会伪装成 api_key 问题的假线索 | 第一批：无依赖，前置于 bug 排查训练（毕业考试第 3 问），本周末前完成；第二批：贯穿，与第 25 项监控埋点衔接 | 第一批半会话；第二批 1 会话 |
| 20 | E 规范 | 生产标准整体审视（并发/配置安全/文件结构；异步改造已单列第 30 项），结构问题靠近部署时裁定；认知架构图随台账同步更新（本人自改，已学讲到内部细节、未学盒子三问） | 全局把关 | 贯穿 | 零散 |
| 21 | F 加码 | K8s（本机 minikube） | 容器编排学习，队尾可砍 | 第 14 项后 | 2~3 会话 |
| 22 | G 安全 | 数据安全加固：前端渲染转义防 XSS、CORS 白名单、数据库最小权限账号、MySQL 定期备份（备份存本地/出版社内网、不出域，呼应第 23 项） | 不注入、不越权、数据不丢 | 第 3、4 项后 | 1 会话 |
| 23 | G 安全 | 数据出域合规：生产版切断外部云 API，全链路切本地模型（推理服务由 Ollama 承载，见 §3 硬件约束） | 出版社"数据不出域"硬要求，靠主线②适配器+本地部署兑现 | 卡主线②，与挂起项同批插回 | 0 会话（切换动作） |
| 24 | D 数据 | 语料入库产品化：建库脚本升级为管理接口（新教材入库自动切块→向量化→存库，增量更新、按教材分库＝每教材一独立索引，与第 15 项统一；图片素材转述入库见第 31 项） | 语料无限预设下，加语料不再手跑脚本；向量化环节复用第 6 项批量 embedding | 第 6、15 项后 | 1 会话 |
| 25 | H 可观测 | 监控告警（Prometheus＋Grafana）：接口延迟、队列积压、模型调用失败率仪表盘；指标由第 19 项埋点或新增 /metrics 端点暴露，Prometheus 抓取 | 日志是出事后查，监控是事前盯住；队列堆死/模型挂了不靠用户投诉发现 | 第 11、19 项后 | 1 会话 |
| 26 | H 可观测 | 压测演练（wrk/locust）：承压层全就位后模拟高并发 | "请求无限"预设的验收考试，压出真实瓶颈在哪一环 | 第 8~14、30 项后，靠近部署（异步改造属承压层，压测须等它就位才压得准） | 1 会话 |
| 27 | G 安全 | 角色权限（RBAC）：users 表加 role 字段＋路由级权限校验＋前端按角色藏入口＋敏感操作审计（谁何时触发微调/入库，留不可抵赖记录） | 首个真岗位：微调入口、语料入库仅高级编辑可见可触发，普通编辑不可见；与第 3 项分工——第 3 管认证＋行级数据归属（只看自己的数据），本项管角色＋功能级授权（能不能用某功能），不重复；安全硬要求：后端必校（直接调接口也拒 403），前端藏入口只是体验；完成后给第 24 项与挂起的微调集成回补权限闸 | 第 3、4 项后 | 1 会话 |
| 28 | C 承压 | 大纲生成流式输出（SSE）：P 加流式变体（DashScope stream 参数）＋SSE 端点＋前端 EventSource 逐字展示 | 聊天级体验硬需求：编辑不用盯黑屏等结果；SSE＝服务器单向推送（HTTP 原生），比 WebSocket 双向通道轻且对位；流式仍走 P，单入口不破 | 第 1、4 项后（前端 EventSource 展示在第 4 项框架重写后做，免在旧前端做两遍） | 半~1 会话 |
| 29 | F 加码 | WebSocket 统一实时消息通道：每登录用户一条常驻连接，承载任务进度/完成通知/审计事件广播 | 学习目的进场（本人明确要求），岗位＝大厂常见的"WebSocket 管消息推送＋SSE 管生成流"分工；严格说推送单向、SSE 可顶，故为加码项，熔断时与第 12、21 项同批优先砍；做时与第 28 项对照学（两者区别当场说清） | 第 28、11 项后 | 半~1 会话 |
| 30 | C 承压 | 异步改造（async/await）：P、RAG 召回、数据库 IO、外部 API 调用等同步阻塞点改异步；流水线内可并行的智能体步骤并发化 | 治本“同步阻塞”——第 8 项多 worker 只是多开进程分担，单 worker 内一个慢请求仍占死；“请求无限”预设下 IO 不阻塞才真扛并发，与第 8 项配套 | 与第 8 项配套，建议在第 8 项前后做 | 2~3 会话 |
| 31 | B 欠账 | 图片素材接 Qwen-VL 转述链（模块 5 扩展方向）：教材图片素材上传后经统一推理入口 P 调 Qwen-VL 转成文字描述，再复用第 24 项入库链向量化进库、参与检索 | 教材含图片素材，图片不进知识库则检索缺图片内容；分工＝Qwen-VL 管图片转述、Qwen Embedding 管文字向量化（见认知架构图） | 第 6、24 项后（转述成文字后复用第 24 项入库链） | 1 会话 |
| 32 | B 欠账 | 负面清单 U、重写循环完善（模块 7 扩展方向）：教案生成后按负面清单 U 自检，不合格触发重写循环（经 P 反复调模型），直到合格或达重写上限 | 一次生成质量不稳，负面清单 U 是质量闸、重写循环是兜底；模块 7 流水线基础版已建（见 §7），此处补到生产可用，与“写作→审核→重写”环节衔接 | 第 1 项后（重写循环反复调 P，靠其超时/重试兜底） | 1 会话 |
| 挂起 | — | P 接主线②适配器 | 卡主线②微调产出，不占位、就绪后插回 | — | — |
| 挂起 | — | 微调任务集成：前端提交入口→任务队列（复用第 11 项机制）→训练脚本（主线②产出）→适配器产物→P 加载 | 卡主线②阶段五/六，与适配器插回同批；产品定位＝系统性改进（攒对照数据批训）非逐篇急救 | 主线②就绪＋第 11 项完成 | — |
执行原则：全部走 B 档（每行说得清才合入）；影响正确性的问题当场修，性能/架构类按台账排期；每项完成后遗留区销账一行；基础设施（MySQL/Redis/APISIX/nginx/Prometheus）一律 Docker 拉起，不装 Windows 原生版；第 0 项建仓时同步锁依赖清单（requirements/pyproject）。
时间预算：第 0 步约 33~38 次会话（≈ 12~13 周，含第 30 项异步改造 2~3 会话、第 31/32 项各 1 会话），之后部署/文档/复盘/毕业另留 ≥ 2 周，阶段七全程约 14~16 周。
熔断条款：外部硬节点＝主线②开学考核（时间未定，以深度学习方案登记为准）；时间一定，备考期本线暂停或压缩会话，阶段七工期顺延；若顺延后算不平，再按序砍队尾（第 21 项）→加码项（第 12、10、29 项），B 组欠账与 C/D/G 承压安全层不动。
排除裁定（✅ 定 2026-09-01）：配置中心/服务注册发现/灰度发布等微服务组件不进表——本项目是单体，这些工具治"服务多到管不过来"的病，单体无岗位；拆微服务之日再登记。服务器成本不属本项目预算：生产硬件由出版社采购（见 §3），学习阶段全部组件开源免费、Docker 本机拉起，笔记本即数据中心。


**AI 使用三档制**：

| 档位 | AI 可以做什么 | 前提条件 |
|---|---|---|
| A 档（教练） | 埋雷、翻译报错、当橡皮鸭 | 默认档，任何时候可用 |
| B 档（扩展） | 把手写通过的最小版扩展成生产版 | 该模块最小版已手写跑通并通过口述验收 |
| C 档（生成） | 直接生成样板代码 | 仅限已能说出该类代码结构的模块，生成后逐行验收 |

红线：**任何档位下，AI 产出的代码没经过你"为什么这么写"的逐行解释，不许合入项目。**

**协作代码验收（✅ 定，2026-08-21）**——项目与他人协作时，别人写的代码按以下规则处理：
1. 别人拿进项目的代码 = C 档生成代码同等待遇：你说不清"为什么这么写"就不许合入（同一红线，对人同样适用）。
2. 分级要求：主线核心模块（阶段六表内 7 个）每行都要说得清——毕业抽问范围；外围模块（协作者的运维脚本、边角胶水）至少说清顶层——干什么的、和哪个接口相接（同"AI 应付项的底线"标准）。
3. 分工先保练习：我正在练的模块必须我写；协作者先写了的，二选一——换练习模块，或对其代码做逐行验收把它"变成"我的。


---


## 2. 时间管理

**开学版（✅ 定，2026-08-31）**：按课表（d:\Learning\课表.md，唯一事实源）核定。学习日＝周一~周五＋周日（周六休息零额度；周日晚百年党史为水课，零投入不计），一天一条线与深度学习线轮转，每次会话 1h 保底、2h 理想 → 本线每周约 3 次会话（3~6h）。节假日（中秋落在第 5 周、国庆落在第 6~7 周）按届时实际放假安排：假期不强制学习，轮转顺延不重置。若算不平→深度学习线降级为只应付社团作业。

会话组成（时长按实际会话时长等比压缩）：

| 环节 | 内容 |
|---|---|
| 开场 | 默写复盘：说不出来的就是今天第一任务 |
| 主体 | 手写 + 埋雷修雷；资料只查马上要用的那一小块 |
| 收尾 | 口述今天学了什么，教练对照当天收件箱条目检查 |

原则：
- 时间花在"卡住-想通"上，不是行数上。10 行真懂 > 200 行抄写。
- 卡住 30 分钟必须停手，写下"期望/实际/猜想"三行再求助。
- **周末加餐＝bug 排查训练**（✅ 定，2026-09-03）：每周固定 1~2 次、每次 ≤1h，挂本时段，不额外占两条主线时间额度（时间账仍按本节「每周约 3 次会话」算平）。规则由 bug 排查师守则（`d:\Learning\.qoder\rules\bug-drill-guidelines.md`）管：在真实项目里注入生产级 bug，本人自己找证据定位，AI 只出题＋监考＋复盘，不给答案。开工前置＝台账第 19 项第一批已落地 ＋ git 工作区干净。原「本周修过的 Bug 换个形式再埋一次」保留为其中一种题型。


---


## 3. 环境与硬件约束

- **后端启动命令（焊死，2026-08-21 随项目目录迁移更新）**：永远从项目根（d:\Learning\pub_of_agent）执行
  `d:/Learning/pub_of_agent/.venv/Scripts/python.exe -m uvicorn rag.app:app`
  禁止 `python rag/app.py` 直接跑（会撞 `'rag' is not a package`）。原理见收件箱《同名模块的身份随启动位置变化》《uvicorn 启动命令的模块路径按执行位置解析》。
- **部署目标：本地化部署＝部署在出版社提供的目标环境（数据不出域），不是跑在自己笔记本上**。学习阶段跑本机小规格 + API；生产硬件（见蓝图 §6 分档）由出版社采购。
- 硬件上限：显存 8G → Ollama 只跑 ≤7B 的 Q4 量化模型（约 5G）；embedding 用轻量版（≤1G）。超规格不考虑。
- 上半场不碰 GPU（mock LLM + TF-IDF 纯 CPU）；Ollama 到下半场接真实 LLM 时才装。
- 教材智能体原型阶段：LLM 走 DashScope API（账号已有/可申请，见蓝图 §9 原型档），不占本地显存；本地模型到蓝图第 2 阶段再引入。
- pip 环境（2026-08-26 实测）：包源＝pypi.org 默认，未配置任何镜像（pip config 无 index-url）；全局缓存目录＝D:\pip-cache（装过的包下次免下载）。已知坑：开着代理软件（虚拟网卡/隧道模式）访问 pypi 会被规则分流拦断，报 SSL UNEXPECTED_EOF_WHILE_READING——先关代理重试；仍不通再用阿里云镜像兜底：`pip install 包名 -i https://mirrors.aliyun.com/pypi/simple/`。
- 微调（fine-tuning）归主线2承担（见深度学习方案阶段五/六），主线1不重复学。


---




---


## 5. 外部项目与学习主线的映射

**总纲：两条学习主线——①全栈+智能体（本方案）；②深度学习（飞桨）。主线上每一行都要说得清；其余事项一律 AI 代写应付。**

| 事项 | 定位 | 投入原则 |
|---|---|---|
| 教材智能体学习项目（本目录 `pub_of_agent`） | 主线①毕业作品 | 按阶段七台账继续推进，慢做细做；知识库搭建＝RAG 升级素材；本地部署＝§3 的 Ollama 部分；适配器微调＝主线②内容，学一次两处用。项目方案见 §1 下半场（仅设计参考，不照搬） |
| 出版社原型工作任务 | 协作型外部项目（✅ 拆分登记，2026-08-26：外部已有全栈原型 publisher-agent-release 部署上云供出版社试用） | 工作代码一律进独立目录，不进 `d:\Learning`；AI 生成须过三档制 B/C 档验收（逐行说得清或至少顶层说得清），绝不似懂非懂交付；协作代码验收规则同等适用。外部原型缺口（无数据库/无鉴权/BM25 检索/无 LoRA）恰为主线①②在练内容，适配器产物可经 P 插入其原型交付 |
| 飞桨/深度学习 | 主线② | 独立项目、独立守则+方案，不与主线①混账 |
| 绘画智能体 | 封存 | 需求未定，零投入 |
| 大创 AGENT | 封存 | 无人催，零投入 |
| 万方 | 最低投入维护 | AI 代写应付，不占学习时长、不算学习成果 |
| 课程/实验报告 | 固定成本 | 固定时段压缩处理，不许侵蚀学习时段 |
| 社团活动 | 事件驱动 | 来了应对，不预留整块时间 |

**风险规则（任务来得比学得早时）**，只有两条路：
1. 先学后做：能纳入主线的就当练习素材；
2. 走三档制 B/C 档：AI 写＋你逐行验收。

绝不允许"似懂非懂接手"。AI 应付项的底线：交出去的项目至少能说出顶层是干什么的。


---


## 6. 踩坑清单（一行索引，格式规则没有我要求不许修改）

> 用途：知识全文进收件箱→归档飞书后，教练在场内就够不着了；本节只存大坑的"一行永久副本"，每次新会话开场和遗留问题区一起查。
> 格式：【日期】症状 ｜ 根因一句话（全文：飞书文档标题）。

- 【2026-08-18】uvicorn 从 rag/ 目录启动 → `'rag' is not a package` ｜根因：模块身份随启动位置变（全文：飞书《同名模块的身份随启动位置变化》《uvicorn 启动命令的模块路径按执行位置解析》；已焊进 §3 启动命令）

进出规则：
- 什么才进：① 卡了 30 分钟以上；② 概念反直觉、容易再踩。普通笔误不进。
- 什么时候进：周末归档收件箱时，教练对每条问一句"要不要升进踩坑清单"，本人拍板。
- 什么时候出：同一坑换形式重埋过、本人修对了，划掉。


---


## 7. 遗留问题（教练登记区）

> 记录"验收没答上的追问"和"阶段遗留事项"，每次新会话开场先查这里。
> 只记任务和问题，不记知识点（知识点进 d:\Learning\feishu_manage\收件箱.md）。
> 简化规则（✅ 定，2026-08-27）：完成的模块只留一行结论（完成时间 + 仍生效的决定），验收过程、收件箱条目清单、过程复盘一律不登记；只记未销账的任务、待确认事项、跨模块决定。


已完成（一行结论，过程细节不登记）：
- ✅ 阶段二/三/四验收全部通过（2026-08-18 ~ 08-21）。
- ✅ 阶段五认知架构图二轮验收通过（2026-08-21）。
- ✅ 模块 1 数据库（2026-08-24）。
- ✅ 模块 2 鉴权（2026-08-24）。
- ✅ 模块 3 前端框架（2026-08-25）。仍生效的决定：范围收缩——只做 Vue CDN 最小重写，组件库与生产版重写留阶段七扩展补齐。
- ✅ 模块 4 多轮上下文（2026-08-26）。仍生效的决定：滚雪球膨胀按最小版认账不修，靠 fetch_history(5) 条数上限兜底；截断/摘要留扩展方向。
- ✅ 模块 5 embedding 升级 + 向量知识库（2026-08-28）。仍生效的决定：embedding 走 DashScope text-embedding-v4；向量库＝项目内 JSON 文件（块文本+向量一起存），不上向量数据库，扩展再换；key 走项目内 .env；逐块调 API，批量调用留扩展方向；旧 TF-IDF/旧 dict 版 cos 已删（用户自删，不留回退）。
- ✅ 模块 6 统一推理入口 P（2026-08-28）。仍生效的决定：所有生成文本只走单一入口 LLM()，走 DashScope OpenAI 兼容 Chat Completions（qwen-plus）；多轮历史以 user/assistant 消息对进 messages，字符串拼接版 build_history_prompt 废弃；取历史留在 app.py，P 内部不读库（保住 P 通用性）；超时/重试、换官方 SDK、接主线②适配器留阶段七扩展。
- ✅ 模块 7 智能体流水线（2026-09-01，阶段六收尾）。仍生效的决定：基座模型 qwen-plus 经 P 走通，适配器就绪后切换（跨线依赖主线②）；查重用手写 n-gram 数重叠（set 去重 + in 命中，重复率保 [0,1]），比对底本＝全库原始语料；职责边界焊死——召回与存库归编排层，写作智能体不做检索；重复退化三件套已修（写作 prompt 篇幅上下限 + 禁复读、temperature 0.5 + repetition_penalty 1.2）；仍欠的账：初稿禁公式是最小版绕法，公式渲染（MathJax 类）留阶段七前端展示时还；语料单薄导致初稿空泛，靠语料铺开解决（主线②素材准备）。

- 【局势登记】（2026-08-26）外部出版社侧原型已部署上云。仍生效的裁定：学习与工作目录分开（见 §5 拆分登记）；毕业作品定位不变；工作任务走协作型外部项目条款。待确认：① 本人是否参与该原型的后续开发维护 ② 出版社素材流向——课标/参考书/范文预计可正常获取（撑住主线② D3 语料第 1、2 类）；第 3 类"AI 初稿→编辑定稿"对照数据能否获取取决于待确认①，拿不到用本人学习项目自产对替代，不阻塞。

- 【API key 配置事件】裁定已进教练守则（扩展须与实际开发对齐；敏感配置走项目内 .env）。
- ✅ 阶段七第 0 项 git 建仓（2026-09-03）。仍生效的决定：.gitignore 挡密钥/环境/缓存/备份/生成物（.env/.venv/__pycache__/backup/vector_store.json）；源码/文档/语料源/规则配置（.qoder）进版本库受保护；commit message 惯例要写具体说明这一版是什么。三问复述已补；远程仓库已建（https://github.com/Zack673-cpu/pub_of_agent.git，main 分支）。
- ✅ 阶段七第 1 项 P 加超时/重试（2026-09-03）。仍生效的决定：P 函数（llm.py）和 get_embedding（rag.py）都加了 timeout=30、retry 3 次、backoff（2^attempt 秒）、raise_for_status、错误处理（区分 Timeout/ConnectionError/HTTPError/KeyError/ValueError）。

- 【下次开工】（2026-09-03 总监督登记）主线①下次会话做完才算销账：
  ③ **台账第 19 项第一批「最小证据链」**（半会话）：它前置于周日的 bug 排查训练，不做就只能练猜。含四件：app.py 异常落日志（终端＋文件）并带上下文、启动时打印关键配置加载结果、错误 detail 可读、rag.py 相对路径改 `__file__` 锚定。
  第 19 项第一批做完若还有时间，才开台账第 2 项「裸 SQL 换 ORM」的头（需 1~1.5 会话，且第 3 项登录注册依赖它，不急于本次）。

